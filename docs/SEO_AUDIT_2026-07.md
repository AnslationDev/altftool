# ALTFTool technical SEO audit — 2026-07-26

Generated by a 52-agent audit (6 dimensions, adversarial verification).
**27 findings confirmed**, 19 refuted and discarded.

Severity: `critical` blocks indexing or produces output Google rejects · `high` sends a materially wrong crawler signal · `medium` is suboptimal.

---

## Already fixed in this pass

| Issue | Where |
|---|---|
| **Sitemap was invalid XML** — 497 unescaped `&` from Firebase Storage image URLs; Google aborted at line 9467 and discovered 0 of 3,313 pages | `src/platform/seo/sitemapXml.js` (new), applied in all 3 sitemap routes |
| Extensions metadata shadowing (critical, below) | `src/app/extensions/[slug]/page.jsx` |
| Exclusivedeals metadata shadowing (critical, below) | 3 × `src/app/exclusivedeals/**/page.jsx` |
| Search Console / Bing verification tokens were unreachable without enabling the whole SEO engine | `src/platform/seo/generateMetadata.js` — now also read from `ALTFT_VERIFY_*` env vars |

Covered by `tests/sitemap-xml-safety.test.mjs` (8 tests) and `tests/seo-verification-tokens.test.mjs` (5 tests), both wired into `npm run validate`.

---

## Outstanding findings

### CRITICAL (2)

#### Hardcoded page-level generateMetadata shadows the layout's per-extension metadata on every /extensions/<slug> ✅ **FIXED**

`altftoolweb/src/app/extensions/[slug]/page.jsx:6` · _canonical-metadata_

**What's wrong.** Two corrections to the claim. (1) Line range: the offending generateMetadata export spans page.jsx lines 4-12, not just line 6 (line 6 is the createPageMetadata call). (2) The claim understates the impact — it omits that the page-level override also defeats the layout's not-found guard. layout.jsx:57-62 returns robots: { index: false, follow: true } when the Firestore document is missing, but page.jsx's createPageMetadata always emits robots.index: true (generateMetadata.js:314-323, since noindex defaults to false), so that noindex is overwritten too. Any nonexistent /extensions/<slug> that reaches this route therefore ships as an indexable page carrying the same generic title. Beyond title/description/og:image, the layout's per-extension `keywords` array (layout.jsx:69-74) is also discarded, replaced by the site-wide keyword list only.

**Evidence.** page.jsx:6-11 → `return createPageMetadata({ title: "Chrome Extension Details & Features | AltFTool Extensions", description: "View Chrome extension details, features, and information on AltFTool...", path: `/extensions/${slug}` });` vs layout.jsx:63-67 → `return createPageMetadata({ title: `${extension.name} – Chrome Extension`, description: extension.description || ..., path: `/extensions/${extension.slug || slug}`, image: extension.image, ... });`

**Fix.** Delete the `generateMetadata` export from altftoolweb/src/app/extensions/[slug]/page.jsx (lines 4-12) so the layout's per-extension metadata is the deepest — and therefore winning — definition. Keep the page as the pure `export default function Page(props) { return <PageView {...props} />; }`.

#### Hardcoded page-level generateMetadata shadows per-brand/per-category metadata across all 72 /exclusivedeals sitemap URLs ✅ **FIXED**

`altftoolweb/src/app/exclusivedeals/[slug]/[id]/page.jsx:6` · _canonical-metadata_

**What's wrong.** Claim is accurate; two small corrections. (1) Line refs: the store detail layout's createPageMetadata call is layout.jsx:21-31 (not 21-32) and the category layout's is layout.jsx:25-36 (not 24-36); the generateMetadata exports in all three page.jsx files span lines 4-12 with the hardcoded title on line 7 (the claim's headline cites [slug]/[id]/page.jsx:6, which is the createPageMetadata call — the literal title string is line 7). (2) The loss is slightly broader than described: because Next.js merges metadata shallowly per top-level field, the page's call also wipes the layout's per-entity `keywords` array (page passes none, so createPageMetadata emits only siteConfig.keywords) in addition to title, description, og:title/description and og:image. Canonicals are unaffected — both layout and page pass the same path. The JSON-LD emitted by the layouts (OfferCatalog / CollectionPage / ItemList / BreadcrumbList) still renders correctly and is per-entity; only the <title>/<meta> tags are shadowed. The second half of the proposed fix (deduplicating /exclusivedeals/<cat>/<id> against /exclusivedeals/store/<cat>/<id> in sitemap.js) is a separate, advisory near-duplicate concern, not part of this defect.

**Evidence.** exclusivedeals/[slug]/[id]/page.jsx:7 → `title: "Brand Deals & Offers | Exclusive Deals"` while exclusivedeals/[slug]/[id]/layout.jsx:27-29 already builds `title: `${brand.brandName} Coupons, Promo Codes & Offers``, `description: brand.about || ...`, `image: brand.imagedeal || brand.img || brand.brandLogo`.

**Fix.** Delete the `generateMetadata` exports from all three page.jsx files (exclusivedeals/[slug]/page.jsx:4-12, exclusivedeals/[slug]/[id]/page.jsx:4-12, exclusivedeals/store/[slug]/[id]/page.jsx:4-12). The sibling layouts already emit correct per-entity title/description/canonical/og:image. Additionally, /exclusivedeals/<cat>/<id> and /exclusivedeals/store/<cat>/<id> describe the same brand from the same db.json record — pick one as canonical and drop the other from sitemap.js:731-739.

### HIGH (14)

#### Sitemap advertises /games, which next.config.mjs 301-redirects to /tools/games

`altftoolweb/src/app/sitemap.js:451` · _indexability_

**What's wrong.** Two corrections to the claim's supporting detail. (1) File path miscited: categoryTaxonomy.js is not in packages/core — it lives at /Users/niki/knworkspace/kn1/altftool/altftoolweb/src/platform/registry/categoryTaxonomy.js, where line 37 is `{ label: "Games", slug: "games", description: "Free browser games — no install, no sign-up." }`, and sitemap.js:2 imports slugifyCategory from "@/platform/registry/categoryTaxonomy". (2) The dead-code side effect is narrower than stated: the redirect source is the exact literal "/games" with no ":path*" suffix, so only the hub page src/app/games/page.jsx is unreachable — the src/app/games/[slug] subtree still resolves normally. Those [slug] pages are, however, absent from the sitemap entirely (the only other "/games" match in sitemap.js is the unrelated import of @/app/altfgame/_data/games at line 49), so the whole /games subtree is orphaned from the sitemap while its hub is advertised as a 301. Severity is high rather than critical: it affects one URL, which Search Console will classify as "Page with redirect" and drop, with no impact on the rest of the sitemap.

**Evidence.** sitemap.js:450-455 `for (const experience of EXPERIENCE_CATALOG) { pushUnique(entries, seen, experience.href, {...}) }`; packages/core/src/experienceCatalog.js:275 `href: "/games"`; next.config.mjs:174-178 `{ source: "/games", destination: "/tools/games", permanent: true }`; sitemap.js:493-498 already pushes `/tools/${category}` including `games` (categoryTaxonomy.js:37 `{ label: "Games", slug: "games" }`).

**Fix.** Either drop /games from EXPERIENCE_CATALOG (packages/core/src/experienceCatalog.js:275) and point that catalog entry at /tools/games, or skip redirect-source paths in the EXPERIENCE_CATALOG loop. Also delete or re-route altftoolweb/src/app/games/page.jsx since the redirect makes it dead code.

#### /bops hub is robots nofollow while every child collection it links to is indexable and in the sitemap

`altftoolweb/src/app/bops/page.jsx:25` · _indexability_

**What's wrong.** app/bops/page.jsx:25 sets `robots: { index: false, follow: false }`, emitting `noindex, nofollow` on the only page that links the Business Ops collections (BUSINESS_OPS_PRODUCTS hrefs rendered at app/bops/page.jsx:98-114). Every other intentionally-noindexed page in this tree uses `follow: true` (loans/lendleap/layout.jsx:4, insurance/homesafe-cover/layout.jsx:4, pet-services/pawsome-grooming/layout.jsx:4), so /bops is the outlier. Correction to the original claim: the blast radius is three hubs, not five. /bops/insurance and /bops/loans still receive follow-able internal links from their own indexable vertical pages via header and footer (insurance/_components/InsuranceHeader.jsx:53 and InsuranceFooter.jsx:20; loans/_components/LoanHeader.jsx:52 and LoanFooter.jsx:20; those verticals are indexable per loans/_lib/metadata.js buildLoanMetadata). Only /bops/pet-services, /bops/legal-services and /bops/senior-care are left with no follow-able internal inbound link at all — their landers are standalone pages that never link back to the hub (grep for those paths inside app/bops/pet-services, legal-services, senior-care returns only each hub's own `path:` in metadata), so those three are discoverable solely via the XML sitemap. Note also that /bops is itself a link sink rather than an orphan: the indexable collection hubs link into it through components/BusinessOpsHeader.jsx:24, so crawl equity flows in and is then dropped. Fix is as proposed: `robots: { index: false, follow: true }`.

**Evidence.** app/bops/page.jsx:25 `robots: { index: false, follow: false },`; app/sitemap.js:414-417 `for (const collection of Object.values(BOPS_COLLECTIONS)) { pushUnique(entries, seen, `/bops/${collection.slug}`, { priority: 0.7, ... }) }`; app/bops/insurance/page.jsx:4-10 and app/bops/loans/page.jsx:4-10 both call createPageMetadata with no noindex; contrast app/bops/loans/lendleap/layout.jsx:4 `robots: { index: false, follow: true, googleBot: { index: false, follow: true } }`.

**Fix.** Change app/bops/page.jsx:25 to `robots: { index: false, follow: true }` to match the rest of the Business Ops tree.

#### /news/headlines and /news/local serve byte-identical content; /news/trending is the same set re-sorted — all three indexable and in the sitemap

`altftoolweb/src/app/news/local/page.jsx:17` · _indexability_

**What's wrong.** Correction 1 (strengthens the claim): TrendingPage's re-sort at news/trending/page.jsx:18-20 is a dead no-op for the rendered output. NewsListing.jsx:294-299 re-sorts the incoming `articles` prop by `published_hours_ago` and derives `topNews = sorted.slice(0,9)` / `moreNews = sorted.slice(9,13)` from that recency sort, discarding the engagement order the page passed in. The sidebar "Trending" list (NewsListing.jsx:303-313) sorts a fresh copy of `articles` by engagement on every route regardless. So /news/trending is not "the same set re-sorted" in the rendered DOM — it is byte-identical to /news/headlines and /news/local in the main grid, sidebar, and More News block.

Correction 2 (narrows "byte-identical"): the three pages are not literally byte-identical documents — they differ in the `<title>`/meta description from createPageMetadata and in the `title`/`description` strings rendered at NewsListing.jsx:360-362 and the duplicate h2 at line 385 ("Headlines" vs "Local News" vs "Trending Now"). Every article card, image, link, and the entire sidebar are identical. That is boilerplate-only differentiation across three self-canonical, indexable, sitemap-submitted URLs.

Correction 3: /news/local is additionally misleading to users and crawlers on its own terms — its title, description, keywords ("local news", "city updates", "community news") and h1 promise geo-scoped content while the feed list is GLOBAL_FEEDS only (getNewsDataServer.js:17), because the `location` branch at lines 19-25 is never reached from this route.

**Evidence.** news/local/page.jsx:17 `const newsData = await getNewsDataServer();` vs news/headlines/page.jsx:17 `const newsData = await getNewsDataServer();`; news/lib/getNewsDataServer.js:10 `const cacheKey = `news:${location ?? "global"}:${topic ?? "all"}`;` (identical key, `location` param never passed by /news/local); sitemap.js:97-101 lists `/news/headlines`, `/news/local`, `/news/trending`; news/topics/[topic]/page.jsx:49 `noindex: true`.

**Fix.** Pass a real filter to /news/local (`getNewsDataServer({ location })`) so it is not a duplicate, or noindex /news/local and /news/trending and drop them from staticRoutes in sitemap.js:97-101, keeping /news/headlines as the canonical listing.

#### All 38 /altflovepdf/<toolSlug> pages emit one identical title and description

`altftoolweb/src/app/altflovepdf/[toolSlug]/page.jsx:9` · _canonical-metadata_

**What's wrong.** Two details in the report are inaccurate. (1) The hardcoded description is 117 characters, not 152: "Free online PDF tool. Convert, merge, split, compress, rotate, protect and unlock PDF files securely in your browser." (2) The emitted document title is not the bare literal — src/app/altflovepdf/layout.jsx:5-8 defines `title: { default: "Altf❤️PDF — 100% Private PDF & Image Tools", template: "%s | Altf❤️PDF" }`, and since resolveDocumentTitle (src/platform/seo/generateMetadata.js:202-225) returns a plain string for this title (no leading/trailing AltFTool brand), the template applies, so every one of the 38 pages emits `<title>PDF Tool — Convert, Merge, Split & More | Altf❤️PDF</title>`. og:title/twitter:title use the untemplated literal. Either way all 38 are identical, so the defect and its severity (high — 38 indexable, sitemap-submitted URLs with duplicate title/description/og/twitter) are unchanged.

**Evidence.** lines 6-14: `export async function generateMetadata({ params }) { const { toolSlug } = await params; return createPageMetadata({ title: "PDF Tool — Convert, Merge, Split & More", description: "Free online PDF tool. Convert, merge, split, compress, rotate, protect and unlock PDF files securely in your browser.", path: `/altflovepdf/${toolSlug}` }); }` — `toolSlug` is used only for `path`.

**Fix.** Mirror altfcalculators/[toolSlug]/page.jsx: look up `const tool = TOOLS.find((item) => item.slug === toolSlug)` inside generateMetadata and pass `title: `${tool?.name || formatToolName(toolSlug)} — Free Online PDF Tool``, `description: tool?.desc || ...`.

#### Every /pranx/<slug> URL in the sitemap is titled "Pranx Studio" with the same description

`altftoolweb/src/app/pranx/[slug]/page.jsx:7` · _canonical-metadata_

**What's wrong.** Scope is smaller than claimed. `export const pranks` in src/app/pranx/data/pranxData.jsx spans lines 47-72 and contains 24 entries, not ~44 — the 44 figure counts `showcaseTiles` (:366-387) too, which sitemap.js never iterates. Of those 24, 23 are single-segment slugs served by [slug]/page.jsx and exactly one (`jurassic-park/console`, :61) is two-segment and served by [slug]/[child]/page.jsx, so the [child] file governs a single sitemap URL rather than plural "nested experiences". So the real impact is 24 sitemap-submitted URLs sharing the title "Pranx Studio | AltFTool" and one meta description. The "no keyword surface" framing is partly overstated: PrankFrame.jsx:38-39 renders `prank.title` as the H1 and `prank.description` beneath it, so framed pranks do have on-page text. It is accurate, however, for the ~12 immersive pranks (standalonePrankComponents at pranxData.jsx:425 plus wideComponents at PranxApp.jsx:9), which bypass PrankFrame entirely and therefore have neither unique metadata nor an H1. The proposed fix is valid: each entry carries its own `title` and `description` fields on the same objects the sitemap iterates.

**Evidence.** lines 4-12: `const { slug } = await params; return createPageMetadata({ title: "Pranx Studio", description: "Original browser prank simulators, screensavers, fake terminals, mini games, and screenshot makers.", path: `/pranx/${slug}` });`

**Fix.** Import `pranks` from ../data/pranxData, resolve the entry by slug in generateMetadata, and emit `title: `${prank.title} — Pranx Studio`` with the prank's own description; fall back to the current constants only when the slug is unknown. Apply the same change to pranx/[slug]/[child]/page.jsx:4-12.

#### /tools/<anything> returns 200 with a self-referencing canonical and a templated title for unknown categories

`altftoolweb/src/app/tools/[category]/page.jsx:98` · _canonical-metadata_

**What's wrong.** Severity is high (unbounded soft-404 / duplicate-content index bloat), not critical — it does not block indexing of legitimate pages. Two corrections to the claim's characterisation of what actually renders: (a) the page is NOT thin or empty. `getInitialToolCatalog(category)` (toolRouteUtils.js:66-108) matches zero tools for an unknown slug, then falls back to TOP_PRIORITY_TOOL_SLUGS + one representative per category + the whole catalog up to `limit = 64`, so /tools/foo renders a ~64-tool grid that is a near-duplicate of /tools/all under a fabricated "Foo Tools" title — duplicate content on top of the soft 404. (b) The emitted JSON-LD is partially self-limiting: `createItemListJsonLd` returns null when zero items match (generateMetadata.js:966), so no empty ItemList is emitted, but `createCollectionPageJsonLd` and `createBreadcrumbJsonLd` (page.jsx:110-126) still emit the fabricated label and a self-referencing @id for the bogus URL. Minor citation fix: the canonical-only tool-URL loop in sitemap.js spans lines 501-510 (the explanatory comment is 501-504), and the /tools/<category> entries come from the loop ending at line 498.

**Evidence.** tools/[category]/page.jsx:85-101 — after the two `redirect()` branches there is no `notFound()`; `const label = formatCategoryLabel(category); const isAll = category === "all"; const path = `/tools/${category}`;` and the component renders. formatCategoryLabel (toolRouteUtils.js:113-120) explicitly title-cases arbitrary input: `String(value).replace(/-/g, " ").replace(/\b\w/g, ...)`.

**Fix.** In the default export, after the redirect branches, call `notFound()` when `category !== "all" && !getCanonicalCategoryBySlug(category)`; and in generateMetadata return `createPageMetadata({ ..., noindex: true })` for the same condition so an on-demand render can never emit an indexable self-canonical.

#### App AggregateRating emits a non-integer reviewCount 1000× smaller than the visible count

`altftoolweb/src/app/apps/[slug]/page.jsx:76` · _structured-data_

**What's wrong.** Accurate, with three refinements. (1) The emitted value is a JSON *string* ("12.4"), not a number — so it is doubly non-conformant with schema.org Integer, though Google tolerates numeric strings for otherwise-valid integers. (2) The visible-text contradiction occurs in two places, not one: AppDetailClient.jsx:87 `({app.reviewCount} Reviews)` and AppDetailClient.jsx:206 `{app.reviewCount} Reviews`. (3) Severity is best stated as high rather than critical: it does not block indexing of the page, it invalidates the AggregateRating node (Google flags non-integer reviewCount and can drop the whole rich result), understates the count by 1000x, and mismatches on-page content. ratingValue ("4.6" as a string) is fine and is not part of the defect.

**Evidence.** apps/[slug]/page.jsx:72-77     aggregateRating: app.rating       ? {           "@type": "AggregateRating",           ratingValue: app.rating,           reviewCount: String(app.reviewCount || "0").replace(/[^0-9.]/g, "") || undefined,  src/app/apps/data/apps.js:15  reviewCount: "12.4K"   ->  emitted as "12.4" src/app/apps/components/AppDetailClient.jsx:87  ({app.reviewCount} Reviews)  ->  visible "12.4K Reviews"

**Fix.** Parse the suffixed magnitude before emitting: expand K/M/B to a real integer (e.g. "12.4K" -> 12400) and emit reviewCount as a Number, or store a raw numeric reviewCount in apps.js and format it only for display. Drop aggregateRating entirely when a trustworthy integer count is unavailable.

#### Twenty tool pages render a second, conflicting SoftwareApplication node alongside the canonical tool entity

`altftoolweb/src/tools/how-lucky-are-you/pages/index.jsx:489` · _structured-data_

**What's wrong.** Count is 21 tool modules, not twenty — the title undercounts by one; the slug list in DETAIL is correct and contains 21 entries. Across those 21 files there are 40 hand-rolled `<script type="application/ld+json">` SoftwareApplication blocks total (how-lucky-are-you x3, which-fantasy-house x4, what-kind-of-genius x3, which-movie-genre x3, which-animal-are-you x3, whats-your-superpower x3, zombie-survival-chance x3, what-type-of-traveler x3, which-time-period x3, the remaining 12 files x1). Because the multi-block files put one copy in each mutually exclusive render branch (quiz / result / intro states), only one extra node is in the DOM at any given moment — so the rendered result is exactly two competing SoftwareApplication entities per URL, not more. Note the route also mounts the same modules at /tools/all/[slug] (src/app/tools/all/[slug]/page.jsx twin), so both URL shapes are affected. Line numbers for the other files (first occurrence): emoji-translator:166, funny-password-generator:134, fortune-cookie-generator:108, logic-gate-simulator:268, pirate-name-generator:58, random-superpower-generator:81, random-villain-name-generator:71, random-emoji-combiner:78, what-type-of-traveler:162, which-mythical-creature:220, whats-your-spirit-element:242, what-kind-of-genius:268, which-fantasy-house:232, whats-your-superpower:174, which-movie-genre:214, wizard-name-generator:63, whats-your-hidden-talent:236, zombie-survival-chance:169, which-animal-are-you:132, which-time-period:200. Severity is high rather than critical: indexing is not blocked; the harm is a second, unidentified SoftwareApplication node whose description and applicationCategory contradict the canonical `#software` entity. Unrelated hit to ignore: src/tools/mime-type-lookup-tool/lib.js:34 contains the string "application/ld+json" only as a MIME-table data row, not a script tag.

**Evidence.** src/tools/how-lucky-are-you/pages/index.jsx:489-501       <script         type="application/ld+json"         dangerouslySetInnerHTML={{           __html: JSON.stringify({             "@context": "https://schema.org",             "@type": "SoftwareApplication",             "name": "How Lucky Are You? Fortune Quiz", ...  vs src/app/tools/[category]/[slug]/page.jsx:51  createToolJsonLd({ slug, tool, category }) which emits "@id": "https://www.altftool.com/tools/all/how-lucky-are-you#software"

**Fix.** Delete the hand-rolled <script type="application/ld+json"> blocks from all twenty src/tools/*/pages/index.jsx modules. The route-level createToolJsonLd() already covers every one of these slugs with a properly identified, publisher-linked node.

#### 60 /exclusivedeals sitemap URLs are soft-404s: the sitemap builds them from numeric brand.id but both routes look up by brand name

`altftoolweb/src/app/sitemap.js:726` · _sitemap-integrity_

**What's wrong.** Claim is correct on all mechanics; two refinements.

(1) The layouts, unlike the page renderers, DO resolve by id correctly: [slug]/[id]/layout.jsx:9-13 and store/[slug]/[id]/layout.jsx:4-8 both do `category?.brands?.find(item => String(item.id) === String(id))` and build per-brand titles plus (in the non-store layout) OfferCatalog + BreadcrumbList JSON-LD at :50-68. Because they resolve successfully, their `robots: { index: false }` fallback (layout.jsx:19-24, store layout:14-19) never fires — so nothing marks these 60 URLs noindex. Worse, the non-store layout ships structured data (OfferCatalog named after the brand, breadcrumb ending in the brand name) on a page whose visible body is the "You might like these" carousel — a structured-data/content mismatch on top of the soft-404.

(2) The "one hardcoded title for all 30" part is accurate but is caused by precedence, not absence: [slug]/[id]/page.jsx:6-11 and store/[slug]/[id]/page.jsx:6-11 export their own generateMetadata with a constant title/description, and same-segment page metadata overrides the layout's per-brand metadata. So the correct per-brand titles computed in the layouts are discarded.

Minor caveat on (a): the not-found branch is gated on `!loading` (BrandDetail.jsx:275), and `loading` only clears once both Firebase subscriptions fire (:184-197). If the Firebase reads fail or never resolve, the component instead renders the full listing shell at :284+ with `selectedBrand === "all"` and an empty offers list. Either way the URL never renders brand-specific content, so the soft-404 conclusion stands.

**Evidence.** Generated output .next/server/app/sitemap.xml.body contains exactly 30 `/exclusivedeals/<cat>/<id>` and 30 `/exclusivedeals/store/<cat>/<id>` locs (e.g. https://www.altftool.com/exclusivedeals/most-popular/1 and .../store/most-popular/1). db.json: 12 categories, 30 (category,brand) pairs, only 8 distinct brand ids; ids '1' and '2' appear in 12 categories each. db.json `store[*].brands[*].brandName` values are 'Amazon', 'Samsung', 'Apple', 'Myntra', ... — never numeric.

**Fix.** Either stop emitting these two URL families from sitemap.js (drop lines 721-741), or change the emitted segment to the value the renderers actually key on — a slugified brand name — and make both routes resolve server-side with notFound() for unknown brands so bad URLs return a real 404 instead of a client-rendered soft-404.

#### Sitemap lists /games, which next.config.mjs 301-redirects to /tools/games

`altftoolweb/src/app/sitemap.js:452` · _sitemap-integrity_

**What's wrong.** Confirmed as stated, with two additions. (1) src/app/games/page.jsx does still exist as a force-static route, so the code reads as if /games were live — but next.config redirects run before filesystem routing, so it is dead code that never serves. That page also sets path: "/games" in createPageMetadata (src/app/games/page.jsx:63) and emits CollectionPage/ItemList/Breadcrumb JSON-LD anchored on /games (lines 82, 87, 93), i.e. a self-canonical and structured data for a URL that only ever 301s — worth removing along with the catalog href. (2) The "only redirected URL in the sitemap" figure is confirmed against the 29 non-parameterized redirect sources (exactly one overlap, /games); parameterized :path* sources were not exhaustively expanded, so treat "only one" as verified for static sources. Severity is high, not critical: it wastes crawl budget and advertises a redirect hop to a URL already listed in the same sitemap, but does not block indexing of /tools/games. The proposed fix is correct — changing experienceCatalog.js:275 to href "/tools/games" also removes the hop from internal linking and /site-map, which consume the same catalog.

**Evidence.** Matched all 3105 emitted paths against the 90 compiled redirect sources from next.config.mjs; exactly one hit: `/games | /games -> /tools/games | permanent: true`. Both `<loc>https://www.altftool.com/games</loc>` (line 1199) and `<loc>https://www.altftool.com/tools/games</loc>` (line 1742) appear in .next/server/app/sitemap.xml.body.

**Fix.** Point the Games Arcade catalog entry at its live URL (`href: "/tools/games"` in packages/core/src/experienceCatalog.js:275), or skip redirected hrefs in the EXPERIENCE_CATALOG loop in sitemap.js. The same catalog feeds internal linking and /site-map, so fixing it at the source removes the redirect hop everywhere.

#### fact-net sitemap is force-dynamic and stamps lastModified: new Date(), so 3 URLs report a brand-new lastmod on every single request

`altftoolweb/src/app/fact-net/sitemap.js:11` · _sitemap-integrity_

**What's wrong.** Claim stands, but the blast radius in the DETAIL should be scoped down from "high" to medium. Two mitigating facts the claimant did not account for: (a) /fact-net/sitemap.xml is not advertised anywhere — src/app/robots.js emits only `${getSiteUrl()}/sitemap.xml` (plus crawl.extraSitemaps, which is inert while the SEO engine is disabled), so Google only sees this file if it is submitted manually or stumbled upon; (b) the same fact-net URLs are already emitted correctly by the main sitemap — src/app/sitemap.js:91-92 lists /fact-net/categories and /fact-net/listings as static routes, and :848 and :858 emit every fact-net category and article with data-derived lastmod and an `undefined` fallback. So this file is a redundant secondary sitemap whose only unique contribution is 3 URLs with a lastmod that advances on every fetch, plus a per-request Lambda render on Amplify for output that never changes. Note also src/app/sitemap.js:91 does not include the bare "/fact-net" path, so /fact-net itself is only present in the volatile-lastmod fact-net sitemap. The proposed fix (drop force-dynamic, use `getContentSource().settings.generatedAt` or omit lastModified — or delete the file as redundant) is sound.

**Evidence.** src/app/fact-net/sitemap.js:5 `export const dynamic = "force-dynamic"`; :11 `lastModified: new Date(),` inside the baseRoutes map. Lines 18 and 25 fall back to `new Date()` too, though those branches are dead today because aiFactsData supplies a fixed generatedAt. By contrast src/app/sitemap.js contains no bare `new Date()` — every lastModified there is data-derived through safeDate().

**Fix.** Drop `export const dynamic = "force-dynamic"` and replace `new Date()` with the content's real timestamp (`getContentSource().settings.generatedAt`), or omit lastModified entirely when there is no real signal. Simplest resolution is to delete the file per the duplication finding.

#### Firebase blog URLs are hard-capped at 500 and the cap is already binding — older posts can never enter the sitemap

`altftoolweb/src/app/sitemap.js:185` · _sitemap-integrity_

**What's wrong.** Confirmed but three details need correcting. (1) Line numbers are off by one: the loop is sitemap.js:318-329 (not 317-328), the Firebase blog push is 528-540, the taxonomy >=2 gates are 566-613 (not 565-612), and listPublicFirestoreDocs is defined at line 291 (not 290). (2) The "~498 from Firebase" arithmetic is slightly wrong in a way that matters for reproducing it: the top-500 window I fetched contains 500 rows but only 497 distinct slugs (3 duplicate slugs exist in the collection), and zero of them collide with the 31 local blogs.json slugs. So the correct accounting is 31 local + ~497-498 unique Firebase = the 528-529 observed, and the loop is exiting on the 500 cap, not on a short page — the ceiling reading is right even though the subtraction was not. Current overflow is 48 posts (548 published vs 500 fetched) and grows with every new post. (3) The listPublicFirestoreDocs sub-claim is real code (pageSize=100 hard-coded at line 291/248-255, payload.documents read at 305 with nextPageToken never consulted) but is NOT currently binding and should be reported as latent rather than active: extensions holds 57 docs, consumerrating/data/brands 10, subcategories 5, categories 16 — all under 100. Only the blog cap is causing live loss today.

**Evidence.** .next/server/app/sitemap.xml.body contains 529 URLs matching /blogs/<single-segment>; blogs.json has 31 entries, so ~498 came from Firebase against a 500 cap. sitemap.js:317 `while (posts.length < maxPosts)` with maxPosts=500; firebaseBlogs.js:239 `orderBy: [{ field: { fieldPath: "createdAt" }, direction: "DESCENDING" }]`.

**Fix.** Raise SEO_FIREBASE_BLOG_LIMIT well above the live post count (or remove the cap and page until an empty response), and add nextPageToken paging to listPublicFirestoreDocs. If the 50,000-URL sitemap limit becomes a concern, split into a sitemap index rather than truncating.

#### All ~21 /tools/<category> pages render one identical hard-coded H1

`altftoolweb/src/app/tools/ToolsClient.jsx:873` · _content-html-semantics_

**What's wrong.** Blast radius is slightly wider than claimed: the same hard-coded H1/lede also ships on the /tools hub itself, because src/app/tools/page.jsx:52 renders MicrotoolClient.jsx, which is a thin wrapper that forwards to the same ToolsClient. So the identical above-the-fold hero appears on /tools, /tools/all, and every /tools/<category> URL (categoryTaxonomy.js lists 22 category labels, so ~22 category routes plus "all" plus the hub, not "~21"). Beyond the title/H1 mismatch, the H1 also contradicts the CollectionPage and ItemList JSON-LD `name` values ("{label} Tools") emitted at [category]/page.jsx:112-119. Note the pages are not wholly duplicate — the tool grid below the fold is genuinely category-filtered via getInitialToolCatalog(category) at [category]/page.jsx:130 — so the defect is correctly scoped to the above-the-fold hero and the missing topical H1 signal, i.e. high severity rather than critical (the pages remain indexable).

**Evidence.** src/app/tools/ToolsClient.jsx:873-877   <h1 className="route-title">     Ready to find your perfect <span className="tp-accent-word">tool?</span>   </h1>   <p className="route-description">     Search {Math.floor(catalogTotal / 50) * 50}+ trusted tools, utilities, and games built to help you work faster.   </p>

**Fix.** Pass the resolved category label into ToolsClient (it already receives `category`) and render `formatCategoryLabel(category)` in the H1/lede, e.g. `"{label} Tools"` for a category and the current copy only when `category` is absent or `"all"`.

#### /smartlink is submitted in the sitemap but is a JS-only redirect to a third-party affiliate URL

`altftoolweb/src/app/sitemap.js:85` · _content-html-semantics_

**What's wrong.** The defect is real but the framing is slightly off in one respect: the URL actually submitted in the sitemap is `/smartlink` with no query string, and that URL does NOT redirect. With no `?mode`, PageView.jsx:45-47 resolves mode to "" and the useEffect (PageView.jsx:49-69) takes no branch, so the page renders the InfiniteLoader forever (PageView.jsx:72-74) — a permanently spinning, zero-content page (only a spinner div and the word "Loading...", no h1/heading/body copy) that has been explicitly submitted for indexing. That is a soft-404 / thin-content signal, not a redirect. The `window.location.replace()` hop to `https://126d3999de4f.sunkissed-morn.com/?p=23600&...` (PageView.jsx:51-60) only fires for `?mode=direct` or `?mode=redirect`, which are query variants Google would only reach if linked, not the canonical submitted URL. The correct severity is high (a content-free, indexable URL submitted in the sitemap and linked from site navigation, on a route whose sole purpose is third-party affiliate monetisation), not critical — nothing here blocks indexing of other pages. Also note the canonical emitted by createPageMetadata is `/smartlink`, so the `?mode=*` monetisation variants all canonicalise back onto this empty page.

**Evidence.** src/app/smartlink/PageView.jsx:50-59     if (mode === "direct") { window.location.replace(REDIRECT_URL); return; }     if (mode === "redirect") { window.location.replace(REDIRECT_URL_WITH_TYPE); return; } src/app/smartlink/PageView.jsx:72-78   if (!mode) { return <InfiniteLoader />; }   if (mode === "direct" || mode === "redirect") { return <InfiniteLoader />; } src/app/smartlink/page.jsx:4-11 — createPageMetadata({ title: "Smart Link", path: "/smartlink" })  // indexable

**Fix.** Remove the `/smartlink` entry from staticRoutes in src/app/sitemap.js and add `noindex: true, follow: false` to its createPageMetadata call in src/app/smartlink/page.jsx.

### MEDIUM (11)

#### Three-hop redirect chain from legacy /housingneeds URLs, terminating on a noindex page

`altftoolweb/next.config.mjs:105` · _indexability_

**What's wrong.** The mechanism and all cited lines are accurate, but two parts of the framing should be corrected:

1. Severity is medium, not high/critical. Google follows up to 5 redirects in a single crawl pass, so a 3-hop chain is still followed and the destination is still reached. The real cost is slower PageRank/signal consolidation and wasted crawl budget (3x the round-trips per legacy URL), not blocked indexing.

2. The noindex terminus is intentional design, not a compounding bug. The docblock at src/app/bops/housing-services/_lib/metadata.js:4-9 states these are deliberately noindex "so they never compete with the editorial Housing Needs guides." So "the whole chain resolves to a page that cannot be indexed" is true as a fact but misleading as an accusation - the correct statement is that three 301 hops are being spent to reach a page that was never meant to be indexed, which makes the chain pure crawl-budget waste rather than a lost ranking signal. The fix worth making is collapsing the hops, not changing the noindex.

Also note the redirect at :132 shadows a route directory that still exists on disk (src/app/bops/housingneeds/roofers/), making that route unreachable - dead code adjacent to, but outside the scope of, this finding.

**Evidence.** next.config.mjs:105 `{ source: "/housingneeds/:category/:path*", destination: "/bops/housingneeds/:category/:path*", permanent: true }`; :112 `{ source: "/bops/housingneeds/roofing/roofers/:path*", destination: "/bops/housingneeds/roofers/:path*", permanent: true }`; :132 `{ source: "/bops/housingneeds/roofers/:path*", destination: "/bops/housing-services/roofers/:path*", permanent: true }`; app/bops/housing-services/_lib/metadata.js:22 `noindex: true`; proxy.js:48 comment "Static path mapping to resolve redirect chains in 1 hop".

**Fix.** Add direct one-hop rules for the legacy entry points (e.g. `/housingneeds/roofing/roofers/:path*` → `/bops/housing-services/roofers/:path*`) above the generic line-105 rule, so no indexed legacy URL needs more than a single 301.

#### Blog taxonomy sitemap thresholds count duplicate posts, so noindex archives can be submitted

`altftoolweb/src/app/sitemap.js:392` · _indexability_

**What's wrong.** Line is sitemap.js:393, not :392 (392 is `: EMPTY_LIVE_COLLECTIONS;`). Two sub-claims are wrong and should be dropped: (1) the Firestore rows are NOT un-normalized — fetchFirebaseBlogsPage already runs them through normalizeBlog (src/app/blogs/data/firebaseBlogs.js:252-256), so getBlogCategories/getAllBlogTags see the same normalized shape the pages do and cannot disagree on which taxonomy slugs exist; the only real divergence is the missing slug dedupe. (2) mergeBlogPosts is NOT already imported in sitemap.js (its blog imports are lines 4-13), so the fix requires adding it to that import block. Impact is also narrower than stated: it produces a sitemap/noindex contradiction only for a taxonomy whose sole unique member is a slug published in both blogs.json and Firestore (plus inflated author postCount thresholds at :595). Note a separate, non-claimed skew in the same pool: the sitemap caps Firestore at SEO_FIREBASE_BLOG_LIMIT=500 (sitemap.js:185, 313-332) while the archive pages use fetchAllFirebaseBlogs, so counts can also diverge downward.

**Evidence.** sitemap.js:392 `const sitemapBlogs = [...getAllBlogs(), ...liveCollections.firebaseBlogs];`; sitemap.js:570-571 `filterBlogsByCategorySlug(sitemapBlogs, categorySlug).length >= 2`, :582-583 for tags, :594 for authors; blogs/category/[category]/page.jsx:38-40 `mergeBlogPosts(getAllBlogs(), firebasePosts)` and :69 `noindex: (categoryPosts?.length || 0) < 2`; blogs/data/blogs.js:445-460 mergeBlogPosts dedupes via `merged.set(normalized.slug, ...)`.

**Fix.** Replace sitemap.js:392 with `const sitemapBlogs = mergeBlogPosts(getAllBlogs(), liveCollections.firebaseBlogs);` (already imported from @/app/blogs/data) so the sitemap's thresholds are computed over the same deduped, normalized set the archive pages use.

#### Every /tradeon/asset/* and /tradeon/chart/* title exceeds 60 characters after the root brand template

`altftoolweb/src/app/tradeon/asset/[symbol]/page.jsx:21` · _canonical-metadata_

**What's wrong.** Corrections to the claim's specifics: (1) "BTCUSDT" is not a route symbol — that is the `binance` pair field; the routed `symbol` values are BTC, ETH, AAPL, EUR/USD, etc. The real length range is asset titles 62 chars (3-char symbols such as BTC) up to 66 chars (7-char FX symbols EUR/USD, GBP/USD, USD/JPY, USD/INR, AUD/USD), and chart titles 66 up to 70 chars — so the claimed 66/70 figures are the maxima, and *every* title in both families still exceeds 60. (2) The stated impact is overstated: the keyword-bearing head ("BTC — Price, Chart, Prediction & Analysis", 41 chars) survives SERP truncation; what gets cut is the duplicated brand tail. The actual defect is the doubled brand suffix "| Tradeon | AltFTool" burning ~20 chars of the title budget on all 72 tradeon instrument URLs, not lost keywords. (3) Scope note: the sibling tradeon pages are unaffected — src/app/tradeon/page.jsx:7, dashboard/page.jsx:7 and workspace/page.jsx:7 use an em-dash "— Tradeon" form and stay within budget after the template appends "| AltFTool".

**Evidence.** tradeon/asset/[symbol]/page.jsx:21 `title: `${symbol} — Price, Chart, Prediction & Analysis | Tradeon`` and tradeon/chart/[symbol]/page.jsx:21 `title: `${symbol} Full Chart — Interactive Market Workspace | Tradeon``; neither ends in "AltFTool", so the layout template at altftoolweb/src/app/layout.jsx:58 adds a second suffix.

**Fix.** Shorten to a single suffix and let the root template supply the brand, e.g. `${symbol} Price & Chart Analysis` and `${symbol} Full Chart Workspace` — or route them through `compactBrandedTitle()` (platform/seo/generateMetadata.js:100) which enforces the 65-char budget.

#### createArticleJsonLd hard-codes author @type Organization, so human news bylines are typed as companies

`altftoolweb/src/platform/seo/generateMetadata.js:833` · _structured-data_

**What's wrong.** Two corrections to the reported detail. (1) Scope is wider than /news: the only other caller, src/app/wattpad/read/[bookSlug]/[chapter]/page.jsx:78-85, passes author: book.authorId — so those chapter Articles emit an opaque user ID typed as Organization, which is doubly wrong (wrong @type AND a machine ID as a display name). Any fix should cover both call sites. (2) The /news byline is worse than a static mistyping: enrichArticle picks the author at random (src/app/news/[slug]/page.jsx:42, `const author = pick(AUTHORS)` over Math.random) on every ISR regeneration (revalidate = 600), so the author name in the JSON-LD is nondeterministic and changes every 10 minutes independently of the article — a fix that only switches @type to Person still leaves an unstable author entity. (3) The stated impact is overstated: Article rich results do not require an author, so this does not block indexing or trigger a Search Console error; it is a wrong-signal defect (Google's Article "author" best-practice guidance explicitly says to use Person for an individual and Organization only for an organization) degrading author-entity/E-E-A-T attribution. Severity high, not critical.

**Evidence.** generateMetadata.js:832-835     author: {       "@type": "Organization",       name: author || siteConfig.name,     },  src/app/news/[slug]/page.jsx:149  author: article.author?.name || article.source, src/app/news/[slug]/page.jsx:21   { name: "Rohan Mehta", designation: "Senior Political Correspondent", ... }

**Fix.** Mirror the createBlogPostingJsonLd logic: accept an authorType (or infer Person when the name differs from siteConfig.name) and emit "@type": "Person" for individual bylines.

#### Tool page BreadcrumbList publishes the raw URL slug as the breadcrumb name

`altftoolweb/src/app/tools/[category]/[slug]/page.jsx:62` · _structured-data_

**What's wrong.** Severity should be medium, not high. The claim's premise — "that string is what Google renders in the SERP breadcrumb trail" — does not hold on this site: buildToolMetadata (src/app/tools/toolRouteUtils.js:187-191) sets path: `/tools/all/${slug}` for EVERY category variant, so every /tools/<category>/<slug> page self-canonicalizes to /tools/all/<slug> (canonical emitted at src/platform/seo/generateMetadata.js:282-283), and src/app/sitemap.js:501-506 intentionally lists only /tools/all/<slug>. The canonical, sitemapped tool URL therefore always takes the `category === "all"` branch and emits the correct "All Tools"; the raw slug appears only on non-canonical duplicate URLs.

The real, code-backed defect is a same-page structured-data / visible-content mismatch: src/app/tools/[category]/[slug]/ToolDetailChrome.jsx:20 renders the visible breadcrumb via formatCategoryLabel(category) ("PDF & Documents"), while page.jsx:62 emits "pdf-documents" in the BreadcrumbList on that same page. Those duplicate URLs are crawlable — src/app/tools/ToolsClient.jsx:648 links tool cards as `/tools/${normalizedCategory}/${slug}` when browsing a category, and src/app/robots.js blocks only /api/ — so Googlebot does fetch markup whose breadcrumb text contradicts the rendered breadcrumb. It is also inconsistent with src/app/tools/[category]/page.jsx:125, which uses the label for the identical crumb.

Count correction: the taxonomy defines 22 canonical categories (src/platform/registry/categoryTaxonomy.js:16-37), not 20, and every one of them differs from the raw slug at least in casing.

**Evidence.** src/app/tools/[category]/[slug]/page.jsx:59-64           createBreadcrumbJsonLd([             { name: "Home", path: "/" },             { name: "Tools", path: "/tools" },             { name: category === "all" ? "All Tools" : category, path: `/tools/${category}` },             { name: tool.name, path: toolPath },           ]),

**Fix.** Use the existing helper: { name: formatCategoryLabel(category), path: `/tools/${category}` } — formatCategoryLabel already returns "All Tools" for the "all" case, so the ternary can go away.

#### Every blog post emits a HowTo whose steps are invented and never appear on the page

`altftoolweb/src/app/blogs/[slug]/page.jsx:214` · _structured-data_

**What's wrong.** Two corrections to the claim's framing. (1) Scope: it is not literally every /blogs/[slug] page. The fabricated HowTo only ships when deriveBlogHowToSteps (src/app/blogs/utils/blogFaq.js:186-192) extracts fewer than 2 steps from the post body; CKEditor/Firebase posts containing a real <ol> or actionable H2/H3 emit steps that do appear in the rendered article. However this is the dominant case, not a corner case — all 31 posts in src/app/blogs/data/blogs.json store plain-prose content with no HTML lists or headings, so every locally-seeded post takes the fabricated branch. (2) Severity: medium, not high or critical. Google deprecated HowTo rich results in 2023, so no rich result is lost and no output is rejected; the actual defect is that the page ships structured data describing steps that are not present in the visible content (a Google structured-data guidelines violation, exposure to a spammy-structured-markup manual action) and mistypes ordinary editorial articles as HowTo entities in the entity graph. The proposed fix is correct: have the fallback branch return [] so createHowToJsonLd's existing empty-steps guard at generateMetadata.js:619 drops the node, and emit HowTo only from author-extracted steps that BlogContent actually renders.

**Evidence.** src/app/blogs/[slug]/page.jsx:214-219           createHowToJsonLd({             path: `/blogs/${slug}`,             name: initialBlog?.heading || ... ,             steps: initialHowToSteps,           }),  src/app/blogs/utils/blogFaq.js:192-204   if (authoredSteps.length >= 2) return authoredSteps.slice(0, 8);   ...   const steps = [     `Read the key points in ${title} and note the workflow that matches your goal.`, ...  grep -rn "initialHowToSteps|howToSteps" src/app/blogs  ->  only page.jsx:197 and :218

**Fix.** Only emit the HowTo when deriveBlogHowToSteps returns author-extracted steps (authoredSteps.length >= 2) AND those steps are rendered on the page; return [] from the fallback branch instead of synthesizing prose, and let createHowToJsonLd's empty-steps guard drop the node.

#### /desktop emits five or more <h1> elements (one per hero slide plus two section headings)

`altftoolweb/src/app/desktop/components/HeroSlider.jsx:87` · _content-html-semantics_

**What's wrong.** The count and one line number are wrong, and understated. The `slides` array in HeroSlider.jsx:6-61 holds **nine** slides ("Tech & Apps", "App on Hand", "Workspace Tech", "Vibrant Tech", "Social Media", "App Focus", "Mobile Lifestyle", "Tech Shot", "Desk Tech"), not five, so the slider alone emits nine `<h1>` elements. With Sidebar.jsx:124 and SpotlightBanner.jsx:51 that is **11** h1 elements on /desktop, not ~7 — eight of the nine slide h1s are visually hidden by opacity but fully present in the server-rendered markup. The sitemap entry is at src/app/sitemap.js:**82** (`{ path: "/desktop", priority: 0.7 }`), not line 79. The metadata title quoted from src/app/desktop/page.jsx:6 is accurate, as is the absence of any h1 matching it. Severity should be medium rather than high: multiple h1s are valid HTML5 and Google states they are not a ranking problem, so the actionable defect is (a) no heading on the page describes the page, and (b) eight off-screen carousel titles are announced as top-level headings to screen readers and heading-outline parsers.

**Evidence.** src/app/desktop/components/HeroSlider.jsx:72-89   {slides.map((slide, index) => (     <div key={index} className={`absolute ... ${index === current ? "opacity-100 z-10" : "opacity-0 z-0"}`}>       ...       <h1 className="section-title text-white ">{slide.title}</h1>  src/app/desktop/DesktopClient.jsx:33-36   <Sidebar apps={topFreeApps} />   // h1 "Top free apps"   <SpotlightBanner />              // h1 "Watch 'Forever After, Never Again'"

**Fix.** Demote the slide headings and the two section headings to h2/h3, and add a single page-level h1 in DesktopClient (or src/app/desktop/page.jsx) matching the metadata title, e.g. "Desktop software for Windows and Mac".

#### /trendingvids renders two competing <h1> elements

`altftoolweb/src/app/trendingvids/components/ContinueWatching.jsx:395` · _content-html-semantics_

**What's wrong.** Two corrections to the reported detail. (1) Severity ceiling: this is medium, not high/critical — it does not block indexing and Google tolerates multiple h1 elements; the concrete defect is a broken heading hierarchy/document outline (and the a11y landmark heading for a personal watch-history strip outranking the page title), not a rejected or mis-indexed page. The supporting evidence that this is a genuine mistake rather than a style choice: every other section on the same page already uses h2 with the identical `section-title` class — VideoGridSection.jsx:93, CategorySection.jsx:15, ExploreVideo.jsx:124 — so ContinueWatching's SectionHeader is the lone outlier. (2) src/app/trendingvids/components/InformativeVideos.jsx:67 contains a third `<h1>`, but that component is never imported anywhere in src/, so it is dead code and does NOT add an h1 to /trendingvids; the page ships exactly two. The proposed fix (change ContinueWatching.jsx:395 to `<h2>`) is correct and visually inert, since `section-title` carries all styling.

**Evidence.** src/app/trendingvids/components/ContinueWatching.jsx:389-396   function SectionHeader({ count, onClearAll }) {     ...             <h1 className="section-title">Continue Watching</h1>  src/app/trendingvids/components/TrendingHero.jsx:497         <h1 className="section-title items-center text-center">Discover <span ...>Trending Videos</span> Instantly

**Fix.** Change the ContinueWatching SectionHeader heading to `<h2>` (the `section-title` class carries the styling, so nothing visual changes).

#### /exclusivedeals server-renders only skeletons — all deal/store content and links are IntersectionObserver-gated

`altftoolweb/src/app/exclusivedeals/DealsClient.jsx:50` · _content-html-semantics_

**What's wrong.** The defect is worse than described for /exclusivedeals and milder for /deals.

(1) It is not "everything after the hero and OutletDealsCard" — the hero is empty server-side too. HeroSection.jsx:20-38 subscribes to Firebase inside useEffect and does `if (loading) return <HeroSkeleton />` (line 39), so it renders a skeleton in SSR as well. The prerendered /exclusivedeals document therefore contains NO page-body content at all: parsing .next/server/app/exclusivedeals.html with scripts/styles stripped yields 3723 chars of visible text, all of it global header nav and footer, plus the single sr-only h1 "Best deals, discounts and product offers" from page.jsx:46. Grep confirms 11 `aria-busy="true"` wrappers (one per RouteLazySection) and 0 `href="/exclusivedeals/..."` anchors.

(2) "None of its internal links" needs qualifying: the 14 category URLs ARE present in the server-rendered ItemList JSON-LD emitted by page.jsx:39-43, and sitemap.js:716 also lists /exclusivedeals/<slug>. So URL discovery is not blocked; what is lost is anchor-based internal linking (link equity and anchor-text context) plus all indexable body copy. Severity is high (materially wrong crawler signal / thin page), not indexing-blocking-critical.

(3) The /deals claim is overstated. DealsHomeClient.jsx renders HeroSection and TopNineShelf directly, outside RouteLazySection; deals.html contains 9 unique `/deals/<slug>` anchors. Only the shelves below them (8 RouteLazySection wrappers) are gated.

(4) The /buysmart claim is confirmed: BuySmartClient.jsx:73-132 gates SearchExplore, Categories, SavingsHub, Trending and FeatureBrand; buysmart.html has 7 aria-busy wrappers and only 1 unique `/buysmart/...` anchor.

**Evidence.** src/components/ui/RouteLazySection.jsx:14-15,60-62   const [visible, setVisible] = useState(false);   ...   <div ref={ref} ...>{visible ? children : fallback}</div>  src/app/exclusivedeals/DealsClient.jsx:50-79   <RouteLazySection fallback={<RouteStripSkeleton items={6} />} minHeight={260}><BrowserCategory/></RouteLazySection>   <RouteLazySection fallback={<RouteCardGridSkeleton cards={6} />} minHeight={520}><Online data={popularSales} /></RouteLazySection>   ... 8 more identical wrappers ...

**Fix.** Render the first two or three content bands (at minimum the category strip and the popular-sales grid that carry the internal links) directly instead of through RouteLazySection, or make RouteLazySection start with `visible` true on the server and only defer after mount — e.g. gate on `content-visibility: auto` (the `.render-deferred` class already used on the homepage, globals.css:1779) which keeps the markup in the DOM.

#### /search-eng has no h1 and no heading elements at all

`altftoolweb/src/app/search-eng/PageView.jsx:157` · _content-html-semantics_

**What's wrong.** The heading defect is real, but two supporting details in the claim are wrong and the severity is medium, not high. (1) The landing content IS server-rendered: .next/server/app/search-eng/ contains only page.js, page.js.nft.json and page_client-reference-manifest.js with no .body prerender, so the route is dynamic SSR and the client component's markup (eyebrow, tagline, CTA chips) ships in the initial HTML — the document is not contentless, it simply contains zero h1-h6 elements. (2) The "no server-rendered result content" point (dataset built inside useEffect in src/app/search-eng/hooks/useSearchDataSource.js:34) applies only to /search-eng?q=... URLs, which are not in the sitemap; only the bare /search-eng landing URL is listed (src/app/sitemap.js:84), so that half of the argument does not affect the indexed URL. (3) Additionally, no heading can come from the global shell because src/platform/navigation/siteRoutes.js:638 lists "/search-eng" in HIDDEN_PUBLIC_SHELL_PREFIXES, suppressing Header and Footer for this prefix — which strengthens the claim. Net defect: an indexable, sitemap-listed page with no h1 and no heading structure at all (anchor: altftoolweb/src/app/search-eng/PageView.jsx:157, landing hero at 246-259).

**Evidence.** src/app/search-eng/PageView.jsx:246-256   <motion.div ... className="se-hero">     <BrandLogo className="se-hero-logo" label="AltFTool Search" />   </motion.div>   <motion.p ... className="se-tagline">One search across <strong>tools, docs, extensions</strong> and the web ...</motion.p>  $ grep -rn "motion.h1\|<h1" src/app/search-eng  ->  (no matches)

**Fix.** Add a real `<h1>` to the landing state (e.g. "Search AltFTool" — it can be visually hidden with `sr-only` next to the BrandLogo), and demote the query echo in the results state to an `<h1>`/`<h2>`. If the route is not meant to rank, drop it from staticRoutes in src/app/sitemap.js and pass `noindex: true` in its createPageMetadata call instead.

#### Root /[slug] dynamic landing page is indexable but has no h1

`altftoolweb/src/app/[slug]/page.jsx:44` · _content-html-semantics_

**What's wrong.** Corrections to the claim: (1) The noindex not-found branch is at page.jsx:35-42, not 34-41. (2) The impact description understates and mis-frames the problem. All four rendered components are "use client" and read Firestore inside useEffect via hooks/data.service.js, so the server-rendered HTML has no content whatsoever: HeroSection.jsx:9 early-returns null, and Section2.jsx:34 renders {title} where title is undefined server-side, producing an empty <h2>; same for Section1Wrapper (Section1.jsx:194-198) and Section3. The page is an empty shell at SSR time, and the h2s only materialise after client hydration — so the defect is "indexable route ships an empty document body", of which the missing h1 is one symptom. (3) The proposed fix does not work: adding the h1 to HeroSection.jsx places it behind `if (!hero) return null;`, so it never renders server-side and disappears entirely whenever the hero doc is absent. The h1 must go in the server component page.jsx wrapper (lines 60-67) with a static string derived from the slug. (4) Severity is medium (suboptimal), not high — an absent h1 is not a broken crawler signal; Google does not require one and Lighthouse's SEO audit does not check for it.

**Evidence.** src/app/[slug]/page.jsx:43-47   return createPageMetadata({     title: `${config.slug.toUpperCase()} Deals & Special Offers`,     description: "Discover curated styles, fresh arrivals, and special promotions on AltFTool.",     path: `/${slug}`,   });  $ grep -rn "<h[1-6]" src/app/[slug]/components/   Section3.jsx:65 <h3 ...   Section3.jsx:92 <h2 ...   Section2.jsx:34 <h2 ...   Section1.jsx:87 <h2 ...   BrandCard.jsx:43 <h3 ...

**Fix.** Add an `<h1>` to src/app/[slug]/components/HeroSection.jsx (or the page wrapper) carrying the same phrase as the generated title, and demote the current top-level `<h2>` in Section2.jsx:34 accordingly.
