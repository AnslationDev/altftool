import { buildServiceMetadata } from "../../_lib/seo";

export const metadata = buildServiceMetadata("roofing", "roofers");

export default function RoofersLayout({ children }) {
  return children;
}
