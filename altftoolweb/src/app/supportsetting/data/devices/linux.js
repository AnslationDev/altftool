import {
  RefreshCw,
  Wifi,
  Volume2,
  Users,
  HardDrive,
  Palette,
  ShieldCheck,
  Cloud,
  Printer,
  LayoutGrid,
  Terminal,
  Cpu,
} from "lucide-react";

// Example, fully-authored guides for Linux desktops — deliberately written
// at the distribution-agnostic level (the core update/network model is the
// same across the major desktop distros) rather than tied to one specific
// distribution's exact menu wording, since "Linux" here covers any of them.
export const linuxSettings = [
  {
    id: "linux-system-update",
    title: "Update System Packages",
    icon: RefreshCw,
    platform: "linux",
    category: "system-updates",
    frequentlyUsed: true,
    recommended: true,
    difficulty: "Beginner-friendly",
    estimatedTime: "4 min read",
    supportedVersions: "Most desktop distributions (Ubuntu, Fedora, Debian, Arch, and derivatives)",
    heading: "Keep your distribution's packages current",
    description:
      "Unlike Windows or macOS, Linux updates are handled by your distribution's package manager rather than one built-in updater — the exact tool differs by distro, but the underlying idea (refresh the package index, then upgrade installed packages) is the same everywhere.",
    details: [
      "Debian/Ubuntu-based systems: `sudo apt update && sudo apt upgrade` from a terminal, or the graphical \"Software Updater\"/\"Update Manager\" app.",
      "Fedora/RHEL-based systems: `sudo dnf upgrade`, or the graphical \"Software\" app's Updates tab.",
      "Arch-based systems: `sudo pacman -Syu`.",
      "Most desktop environments also show a notification icon when updates are available.",
    ],
    important: "A kernel update usually requires a reboot to actually take effect, even though the update itself completes without one.",
    redirectUrl: "https://docs.kernel.org/",
    actionLabel: "Open Kernel & Distro Documentation",
    whyItMatters:
      "Distros ship security patches through the same package manager as everything else — deferring updates leaves known vulnerabilities unpatched, and skipping too many releases in a row can turn a routine upgrade into one with much larger version jumps to reconcile at once.",
    bestPractices: [
      "Update at least weekly, and right away for anything flagged as a security update.",
      "Reboot after a kernel or graphics-driver update rather than leaving the old one running until your next restart.",
      "Read the changelog before a major distro version upgrade (e.g. Ubuntu 22.04 → 24.04) — these are bigger jumps than a routine package update.",
    ],
    commonIssues: [
      { issue: "\"Held broken packages\" or dependency conflict during upgrade", fix: "Run your package manager's built-in fix command first (e.g. `sudo apt --fix-broken install` on Debian/Ubuntu) before retrying the upgrade." },
      { issue: "Update seems to hang with no output", fix: "It's often waiting on a package-manager lock held by another process (e.g. a background update check) — wait a minute, or check for a stuck `apt`/`dpkg` process before force-killing anything." },
    ],
    tipsAndTricks: ["Most distros can be configured to install security updates automatically in the background — worth turning on even if you keep feature updates manual."],
    faqs: [{ q: "Do I need to update every app individually?", a: "No — apps installed through your distro's package manager update together with the system. Apps installed via Flatpak or Snap update separately through their own tools (`flatpak update` / `snap refresh`)." }],
    relatedSettingIds: ["linux-wifi-network"],
  },
  {
    id: "linux-wifi-network",
    title: "Wi-Fi & Network Connection",
    icon: Wifi,
    platform: "linux",
    category: "connectivity-network",
    frequentlyUsed: true,
    difficulty: "Easy",
    estimatedTime: "3 min read",
    supportedVersions: "Most desktop distributions using NetworkManager",
    heading: "Connect to Wi-Fi and manage network connections",
    description:
      "Most modern desktop distros use NetworkManager under the hood, accessed through the network icon in the top or bottom system tray, or each desktop environment's own Settings app (GNOME Settings, KDE System Settings, etc.).",
    details: [
      "Click the network icon in the system tray to see and join available Wi-Fi networks.",
      "Saved networks, VPN connections, and proxy settings live under the same panel's \"Network\" or \"Wi-Fi\" section.",
      "`nmcli` and `nmtui` provide the same functionality from a terminal, useful on systems without a graphical desktop.",
    ],
    redirectUrl: "https://networkmanager.dev/docs/admins/",
    actionLabel: "Open NetworkManager Documentation",
    whyItMatters:
      "Wi-Fi driver support is the single most common source of first-boot friction on Linux laptops — knowing whether your system uses NetworkManager (and how to check connection status from a terminal) makes troubleshooting far faster than guessing through a GUI that might not even be loading correctly yet.",
    bestPractices: [
      "Prefer a wired connection for the initial install/update on a new machine, since Wi-Fi drivers occasionally need a kernel update to work fully.",
      "Keep VPN client packages updated through your package manager rather than a separate manual install where possible.",
    ],
    commonIssues: [
      { issue: "Wi-Fi adapter isn't detected at all", fix: "Check if it needs a proprietary driver (common on Broadcom and some Realtek chipsets) — most distros have an \"Additional Drivers\" tool or a documented package name for this." },
      { issue: "Connects but internet doesn't work (captive portal networks)", fix: "Open a browser and try to load any http (not https) page — many distros' captive-portal detection is less reliable than Windows/macOS and needs a manual nudge." },
    ],
    tipsAndTricks: ["`nmcli device wifi list` and `nmcli device status` are the fastest way to check what NetworkManager sees when the GUI applet isn't showing what you expect."],
    faqs: [{ q: "Why does my Wi-Fi disconnect when I close the lid?", a: "This is a power-management setting, not a network issue — check your desktop environment's power settings for a \"Wi-Fi power saving\" toggle." }],
    relatedSettingIds: ["linux-system-update"],
  },
  {
    id: "linux-display-graphics-drivers",
    title: "Display & Graphics Drivers",
    icon: Cpu,
    platform: "linux",
    category: "display-sound-notifications",
    frequentlyUsed: true,
    recommended: true,
    difficulty: "Intermediate",
    estimatedTime: "6 min read",
    supportedVersions: "Most desktop distributions (Ubuntu, Fedora, Debian, Arch, and derivatives)",
    heading: "Get the right GPU driver running for your hardware",
    description:
      "Graphics driver setup is the single biggest source of Linux desktop friction, especially on NVIDIA hardware — the open-source nouveau driver loads by default but the proprietary NVIDIA driver is usually what you actually want for full performance, hardware video decode, and multi-monitor/mixed-refresh-rate support.",
    details: [
      "NVIDIA: Ubuntu/Debian — `sudo apt install nvidia-driver-<version>` or use the \"Additional Drivers\" GUI tool (Software & Updates); Fedora — enable RPM Fusion then `sudo dnf install akmod-nvidia`; Arch — `sudo pacman -S nvidia nvidia-utils` (or `nvidia-open` for Turing+ GPUs).",
      "AMD and Intel GPUs use open-source kernel drivers (amdgpu, i915) that ship with the kernel — no separate install is normally needed, just keep the kernel and mesa packages updated.",
      "Check what's currently loaded with `lspci -k | grep -A 3 VGA` and confirm the driver in use with `glxinfo | grep \"OpenGL renderer\"` (from the mesa-utils/mesa-demos package).",
      "Wayland sessions need a reasonably recent proprietary NVIDIA driver (535+) for full support — older NVIDIA driver versions work more reliably under the Xorg session instead.",
    ],
    important: "Never mix nouveau and the proprietary NVIDIA driver — blacklist nouveau (most installers do this automatically) or you'll get boot loops or a black screen after login.",
    redirectUrl: "https://wiki.archlinux.org/title/Main_page",
    actionLabel: "Open Graphics Driver Documentation",
    whyItMatters:
      "The wrong or missing driver shows up as tearing, capped resolution, GPU-accelerated apps falling back to software rendering, or external monitors not being detected at all — and on laptops with hybrid NVIDIA+Intel graphics (Optimus), it directly affects battery life.",
    afterImageContent: {
      heading: "Verify the driver took effect",
      steps: [
        "Reboot after installing or switching a graphics driver — driver changes don't apply live.",
        "Run `nvidia-smi` (NVIDIA) or `glxinfo | grep renderer` to confirm the expected driver and GPU are reporting correctly.",
        "Test a demanding app or `glxgears` briefly to confirm acceleration is actually active, not just installed.",
      ],
    },
    bestPractices: [
      "On laptops with switchable graphics, use your distro's hybrid-graphics tool (e.g. `optimus-manager`, `envycontrol`, or GNOME's built-in \"Launch using Discrete Graphics\") rather than forcing one GPU globally.",
      "Hold off on the very latest driver release for a few days after a major distro or kernel update — driver/kernel mismatches are the most common cause of post-update black screens.",
      "Keep a spare TTY escape route in mind (Ctrl+Alt+F3) in case a bad driver leaves the graphical session unusable.",
    ],
    commonIssues: [
      { issue: "Black screen or login loop after installing the NVIDIA driver", fix: "Boot into a TTY (Ctrl+Alt+F3), confirm nouveau is blacklisted, and check `journalctl -b -1` for the specific driver load failure before reinstalling." },
      { issue: "External monitor not detected or stuck at low resolution", fix: "Run `xrandr` (Xorg) or check Settings > Displays (Wayland) — confirm the cable/port supports the resolution, and that the correct GPU output is being used on hybrid-graphics laptops." },
    ],
    tipsAndTricks: ["`inxi -G` gives a quick one-line summary of your GPU, driver, and display resolution — handy for support forum posts."],
    faqs: [{ q: "Do I need the proprietary NVIDIA driver, or is nouveau good enough?", a: "Nouveau is fine for basic desktop use and older cards, but lacks full 3D performance, hardware video decode, and multi-monitor variable refresh rate support — install the proprietary driver for gaming, creative work, or external displays." }],
    relatedSettingIds: ["linux-desktop-environment-customization", "linux-troubleshooting-boot-issues"],
  },
  {
    id: "linux-sound-audio",
    title: "Sound & Audio (PipeWire/PulseAudio)",
    icon: Volume2,
    platform: "linux",
    category: "display-sound-notifications",
    frequentlyUsed: true,
    difficulty: "Easy",
    estimatedTime: "4 min read",
    supportedVersions: "Most desktop distributions (Ubuntu, Fedora, Debian, Arch, and derivatives)",
    heading: "Manage audio devices, volume, and the sound server",
    description:
      "Most current distros (Fedora, Ubuntu 22.10+, recent Arch installs) now default to PipeWire as the audio (and video) server, usually with the pipewire-pulse compatibility layer so PulseAudio-based apps keep working unmodified; older systems still run PulseAudio directly.",
    details: [
      "GUI volume/device control is the same either way — the speaker icon in the system tray, or Settings > Sound in GNOME/KDE.",
      "Check which sound server is active with `pactl info` — the \"Server Name\" line reports \"PulseAudio (on PipeWire ...)\" if PipeWire is running the compatibility layer.",
      "Install/switch on Ubuntu/Debian: `sudo apt install pipewire-audio pipewire-pulse`; Fedora ships PipeWire by default; Arch: `sudo pacman -S pipewire pipewire-pulse wireplumber`.",
      "`pavucontrol` (PulseAudio Volume Control) works against both PipeWire and PulseAudio and is the most reliable place to fix per-app output routing and input levels.",
    ],
    redirectUrl: "https://pipewire.org/",
    actionLabel: "Open PipeWire Documentation",
    whyItMatters:
      "Audio routing is one of the more fragmented parts of the Linux desktop stack — knowing whether you're on PipeWire or PulseAudio (and that WirePlumber is PipeWire's session manager) saves a lot of time chasing fixes written for the wrong stack.",
    bestPractices: [
      "Use `pavucontrol` for per-application volume and device routing rather than hunting through each app's own settings.",
      "If migrating an older system to PipeWire, remove `pulseaudio` and `pulseaudio-utils` packages rather than running both stacks side by side.",
      "For pro-audio/low-latency work, PipeWire's JACK compatibility layer (`pipewire-jack`) usually removes the need for a separate JACK install.",
    ],
    commonIssues: [
      { issue: "No sound after a fresh install or after suspending", fix: "Restart the audio stack with `systemctl --user restart pipewire pipewire-pulse wireplumber` (or `pulseaudio -k` on PulseAudio systems, which auto-restarts it)." },
      { issue: "Bluetooth headphones connect but no audio", fix: "Confirm the Bluetooth A2DP profile is selected in `pavucontrol`'s Configuration tab, and that `pipewire-pulse`/`pulseaudio-module-bluetooth` is installed." },
    ],
    tipsAndTricks: ["`wpctl status` gives a quick tree view of every PipeWire audio input/output and which app is bound to which device."],
    faqs: [{ q: "Should I switch from PulseAudio to PipeWire manually?", a: "Only if your distro hasn't already — most mainstream distros migrated by default in 2022–2023. If audio works fine today, there's no urgent need to switch it yourself." }],
    relatedSettingIds: ["linux-display-graphics-drivers", "linux-desktop-environment-customization"],
  },
  {
    id: "linux-user-accounts-permissions",
    title: "User Accounts & Permissions",
    icon: Users,
    platform: "linux",
    category: "accounts-sync-family",
    frequentlyUsed: false,
    difficulty: "Intermediate",
    estimatedTime: "5 min read",
    supportedVersions: "Most desktop distributions (Ubuntu, Fedora, Debian, Arch, and derivatives)",
    heading: "Manage user accounts, sudo access, and group membership",
    description:
      "Linux permissions are built around users and groups rather than per-app toggles — adding a user to a group (like `sudo`, `wheel`, or `docker`) is how most access is granted, and it's managed the same way whether you're using a GUI Settings panel or the command line.",
    details: [
      "GUI: Settings > Users (GNOME) or System Settings > Users (KDE) to add accounts, set admin status, and manage login options.",
      "Add a user to a group: `sudo usermod -aG <group> <username>` (Debian/Ubuntu use the `sudo` group for admin rights; Fedora/Arch use `wheel`) — the user must log out and back in for new group membership to take effect.",
      "List a user's current groups: `groups <username>` or `id <username>`.",
      "Edit sudo rules directly (advanced) with `sudo visudo`, which validates syntax before saving to avoid locking yourself out.",
    ],
    important: "Always keep at least one working admin/sudo-capable account — removing sudo rights from every account, or a syntax error saved outside of `visudo`, can lock you out of admin actions entirely.",
    redirectUrl: "https://wiki.archlinux.org/title/Main_page",
    actionLabel: "Open Users & Groups Documentation",
    whyItMatters:
      "Most \"permission denied\" support requests trace back to a missing group membership (docker, dialout, plugdev) rather than a broken system — understanding groups turns a confusing error into a one-line fix.",
    bestPractices: [
      "Use a personal admin account for daily work rather than logging in as root directly — `sudo` on a per-command basis is safer and keeps an audit trail.",
      "Log out and back in (or reboot) after any group membership change — it does not apply to already-open sessions.",
      "Review `/etc/sudoers.d/` for any custom rules before troubleshooting unexpected permission behavior.",
    ],
    commonIssues: [
      { issue: "\"user is not in the sudoers file\" error", fix: "From an existing admin account, run `sudo usermod -aG sudo <username>` (or `wheel` on Fedora/Arch); if no admin account exists, this needs to be fixed by booting into recovery/single-user mode." },
      { issue: "Newly added group membership doesn't seem to work", fix: "Log out completely and back in (a new terminal alone isn't enough in most cases) — group membership is set at login time." },
    ],
    tipsAndTricks: ["`getent group <groupname>` shows every member of a group system-wide, useful for auditing who has docker or sudo access."],
    faqs: [{ q: "What's the difference between `sudo` and logging in as root?", a: "`sudo` runs a single command with elevated privileges and logs who ran it; logging in as root keeps the whole session privileged, which is easier to make a costly mistake in and is disabled by default on most desktop distros." }],
    relatedSettingIds: ["linux-firewall-security", "linux-backup-snapshots"],
  },
  {
    id: "linux-storage-disk-management",
    title: "Storage & Disk Management",
    icon: HardDrive,
    platform: "linux",
    category: "storage-backup-data",
    frequentlyUsed: false,
    difficulty: "Intermediate",
    estimatedTime: "5 min read",
    supportedVersions: "Most desktop distributions (Ubuntu, Fedora, Debian, Arch, and derivatives)",
    heading: "Check disk usage, manage partitions, and mount drives",
    description:
      "Disk and partition management on Linux can be done entirely from the command line or through a graphical tool like GNOME Disks or KDE Partition Manager — both talk to the same underlying tools (`fdisk`, `parted`, `mkfs`), so it's fine to mix and match.",
    details: [
      "Check free space: `df -h` for mounted filesystems, `du -sh <folder>` to see what's taking up space in a specific directory.",
      "List all drives and partitions: `lsblk` (tree view) or `sudo fdisk -l` (detailed).",
      "Graphical partitioning: GNOME Disks (`gnome-disk-utility`, preinstalled on most GNOME systems) or GParted (`sudo apt install gparted` / `sudo dnf install gparted` / `sudo pacman -S gparted`).",
      "Mount a drive manually: `sudo mount /dev/sdXN /mnt/mountpoint`; for a permanent mount at boot, add an entry to `/etc/fstab` (back it up first — a bad fstab entry can stop the system from booting).",
    ],
    important: "Never repartition or format a drive that's currently mounted and in use — unmount it first (`sudo umount /dev/sdXN`), and always double-check the device name before running any destructive command.",
    redirectUrl: "https://wiki.archlinux.org/title/Main_page",
    actionLabel: "Open Disk & Partition Documentation",
    whyItMatters:
      "Running out of disk space silently breaks updates, app installs, and sometimes the desktop session itself (a full home partition can prevent login) — knowing how to quickly find what's consuming space avoids a much more confusing troubleshooting session later.",
    afterImageContent: {
      heading: "Confirm a new mount is set up correctly",
      steps: [
        "Run `lsblk` to confirm the partition shows the expected mountpoint.",
        "Run `df -h` and check the filesystem appears with the expected available space.",
        "If you edited `/etc/fstab`, run `sudo mount -a` to test it without rebooting — errors here mean fixing it now instead of at next boot.",
      ],
    },
    bestPractices: [
      "Keep at least 10-15% of your root/system partition free — package managers and journaling filesystems both need working room.",
      "Use `lsblk -f` before formatting anything to double-check UUIDs and confirm you're targeting the right device.",
      "Prefer ext4 or btrfs for the system partition on most desktop setups — btrfs adds native snapshot support if you want that later.",
    ],
    commonIssues: [
      { issue: "\"No space left on device\" but `df -h` shows free space", fix: "You've likely run out of inodes, not space — check with `df -i`; this is common with directories containing huge numbers of small files (e.g. mail spools, node_modules)." },
      { issue: "External drive doesn't mount automatically", fix: "Check it's formatted with a filesystem Linux reads natively (ext4, exFAT with `exfatprogs` installed, or NTFS with `ntfs-3g`) — some distros need the exFAT/NTFS driver packages installed separately." },
    ],
    tipsAndTricks: ["`ncdu` gives an interactive, much faster way to drill into what's eating disk space than repeated `du` commands."],
    faqs: [{ q: "Is it safe to resize a partition without losing data?", a: "Shrinking or growing a partition is generally safe with modern tools like GParted as long as you don't interrupt the process, but always back up first — a power loss mid-resize can corrupt the filesystem." }],
    relatedSettingIds: ["linux-backup-snapshots", "linux-system-update"],
  },
  {
    id: "linux-desktop-environment-customization",
    title: "Desktop Environment Customization",
    icon: Palette,
    platform: "linux",
    category: "personalization",
    frequentlyUsed: true,
    difficulty: "Easy",
    estimatedTime: "4 min read",
    supportedVersions: "Most desktop distributions (Ubuntu, Fedora, Debian, Arch, and derivatives)",
    heading: "Customize themes, extensions, and desktop behavior",
    description:
      "Desktop environments (GNOME, KDE Plasma, XFCE, Cinnamon, etc.) each have their own settings apps and customization ecosystem — GNOME leans on extensions and GNOME Tweaks for anything beyond the basics, while KDE Plasma exposes most customization directly in System Settings.",
    details: [
      "GNOME: install \"GNOME Tweaks\" (`sudo apt install gnome-tweaks` / `sudo dnf install gnome-tweaks` / `sudo pacman -S gnome-tweaks`) for fonts, window behavior, and extension management beyond what Settings exposes.",
      "GNOME extensions are managed through extensions.gnome.org (via a browser connector) or the \"Extension Manager\" app, which also works offline.",
      "KDE Plasma: System Settings > Appearance covers themes, icons, and widgets directly, plus Global Themes for one-click full look changes.",
      "XFCE/Cinnamon/MATE: Settings Manager/System Settings covers panels, themes, and window manager behavior in one place, closer to a classic desktop layout.",
    ],
    redirectUrl: "https://help.gnome.org/",
    actionLabel: "Open Desktop Environment Documentation",
    whyItMatters:
      "The desktop environment is what most people actually mean by \"Linux\" day-to-day — knowing which DE you're running (`echo $XDG_CURRENT_DESKTOP`) determines which settings app, keyboard shortcuts, and troubleshooting guides actually apply to you.",
    bestPractices: [
      "Stick to extensions/themes still maintained for your current GNOME/Plasma version — an outdated extension is one of the most common causes of a broken top bar or crashed shell after an update.",
      "Back up your dotfiles or use your DE's built-in export (KDE's \"Global Theme\" export, GNOME's dconf dump) before heavy customization so it's reproducible on a reinstall.",
      "Test major theme or extension changes before relying on them for daily work — a bad shell extension can occasionally crash the whole desktop session.",
    ],
    commonIssues: [
      { issue: "GNOME Shell crashes or top bar disappears after an update", fix: "Update or disable extensions first — press Alt+F2, type `r`, Enter to restart GNOME Shell (Xorg only; Wayland sessions need to log out and back in)." },
      { issue: "Installed theme/icons don't show up in the picker", fix: "Confirm they're installed in `~/.themes` or `~/.icons` (create the folder if missing) rather than a random download location, then restart the settings app." },
    ],
    tipsAndTricks: ["`dconf-editor` exposes every GNOME setting, including ones with no GUI toggle — useful for advanced tweaks, but change with care since it edits raw configuration."],
    faqs: [{ q: "Can I switch desktop environments without reinstalling Linux?", a: "Yes — most distros let you install a second DE alongside the first (e.g. `sudo apt install kde-plasma-desktop` on an Ubuntu/GNOME system) and pick between them at the login screen, though mixing many DEs long-term can leave leftover packages and conflicting settings." }],
    relatedSettingIds: ["linux-sound-audio", "linux-flatpak-snap-apps"],
  },
  {
    id: "linux-firewall-security",
    title: "Firewall & Security",
    icon: ShieldCheck,
    platform: "linux",
    category: "privacy-permissions",
    frequentlyUsed: false,
    recommended: true,
    difficulty: "Intermediate",
    estimatedTime: "5 min read",
    supportedVersions: "Most desktop distributions (Ubuntu, Fedora, Debian, Arch, and derivatives)",
    heading: "Configure the firewall and harden basic system security",
    description:
      "Most desktop distros ship a firewall tool that isn't enabled by default out of the box on some systems — Ubuntu-based distros use `ufw` (Uncomplicated Firewall), Fedora and most RHEL-based systems use `firewalld`, and both are front-ends over the same underlying kernel netfilter/nftables rules.",
    details: [
      "Ubuntu/Debian: `sudo ufw enable` turns it on with a default deny-incoming/allow-outgoing policy; `sudo ufw status verbose` shows current rules; `sudo ufw allow <port>` opens a specific port.",
      "Fedora/RHEL-based: `sudo systemctl enable --now firewalld`; manage zones and rules with `sudo firewall-cmd --list-all` and `sudo firewall-cmd --add-service=<service> --permanent`.",
      "GUI options exist too — `gufw` for ufw, and firewalld has a GTK config tool (`firewall-config`) for a visual view of zones and rules.",
      "Check what's actually listening on the network with `sudo ss -tulpn` — useful for confirming a firewall rule is blocking (or not blocking) the right thing.",
    ],
    important: "Opening a port for one app (e.g. file sharing, a local dev server) means it's reachable from your whole local network, not just the app that needed it — narrow the rule to a specific source IP/subnet when possible instead of a blanket allow.",
    redirectUrl: "https://firewalld.org/documentation/",
    actionLabel: "Open Firewall Documentation",
    whyItMatters:
      "A default-open firewall configuration (or none running at all) is one of the more overlooked gaps on a fresh Linux desktop install — most distros are reasonably secure by default otherwise, but leaving the firewall off removes one meaningful layer of protection on public or untrusted networks.",
    bestPractices: [
      "Enable the firewall by default and only open the specific ports/services you actually use, rather than disabling it for convenience.",
      "Use your desktop environment's network profile (public vs. home/trusted) if it offers one — some auto-adjust firewall zones based on the connected network.",
      "Keep SSH access restricted to key-based auth if you enable a remote SSH server, and consider changing it off the default port 22 as a light deterrent (not a substitute for real hardening).",
    ],
    commonIssues: [
      { issue: "Local network file sharing or a dev server isn't reachable from another device", fix: "Check `sudo ufw status` / `sudo firewall-cmd --list-all` for the required port or service, and add an allow rule rather than disabling the firewall entirely." },
      { issue: "Firewall rules don't survive a reboot", fix: "Rules added with `firewall-cmd` need `--permanent` (and a `--reload`) to persist — without it, they only apply to the current session." },
    ],
    tipsAndTricks: ["`sudo ufw app list` shows pre-defined application profiles (like OpenSSH) so you can allow a known service by name instead of guessing the port number."],
    faqs: [{ q: "Do I need antivirus software on Linux?", a: "Not for the same reasons as Windows — Linux desktop malware is far less common — but a firewall, prompt updates, and caution about what you install still matter, especially if you share files with Windows/macOS users (where a Linux-hosted antivirus like ClamAV can help scan for malware relevant to them)." }],
    relatedSettingIds: ["linux-user-accounts-permissions", "linux-wifi-network"],
  },
  {
    id: "linux-backup-snapshots",
    title: "Backup & Snapshots",
    icon: Cloud,
    platform: "linux",
    category: "storage-backup-data",
    frequentlyUsed: false,
    difficulty: "Intermediate",
    estimatedTime: "5 min read",
    supportedVersions: "Most desktop distributions (Ubuntu, Fedora, Debian, Arch, and derivatives)",
    heading: "Set up backups and system snapshots before something goes wrong",
    description:
      "Linux offers several backup layers that solve different problems — file-level backups for your personal data, and filesystem or system-level snapshots that let you roll back an entire update or configuration change in minutes.",
    details: [
      "Timeshift is the most common GUI snapshot tool — supports rsync-based snapshots (any filesystem) or native btrfs snapshots (much faster, near-instant) if your root is on btrfs. Install via `sudo apt install timeshift` / `sudo dnf install timeshift` / `sudo pacman -S timeshift`.",
      "Btrfs users can also snapshot manually with `sudo btrfs subvolume snapshot / /.snapshots/pre-update` before a risky change, and many distros (openSUSE, some Fedora spins) auto-snapshot before every package transaction via `snapper`.",
      "For personal file backups, `rsync -av --delete /home/user/ /path/to/backup/` is the classic command-line approach; `Deja Dup` (GNOME) or `KBackup` (KDE) provide a GUI with scheduling.",
      "Cloud sync tools (Nextcloud client, rclone for most cloud providers) cover offsite backup, but should complement rather than replace a local backup.",
    ],
    important: "A snapshot of your system is not the same as a backup of your personal files — most snapshot tools exclude /home by default, so pair system snapshots with a separate file backup strategy.",
    redirectUrl: "https://documentation.suse.com/sles/15-SP5/html/SLES-all/cha-snapper.html",
    actionLabel: "Open Snapshot & Backup Documentation",
    whyItMatters:
      "The single most common reason people lose confidence updating Linux is fear of an update breaking the system with no way back — a working snapshot setup turns that into a two-minute rollback instead of a reinstall.",
    afterImageContent: {
      heading: "Confirm your backup or snapshot actually restores",
      steps: [
        "Take a test snapshot or backup and verify it appears with the expected size and timestamp.",
        "For Timeshift, use the \"Restore\" preview to confirm which files would be affected before you actually need to use it in an emergency.",
        "Test that backed-up files can genuinely be opened from the backup location, not just that the backup job reported success.",
      ],
    },
    bestPractices: [
      "Take a snapshot right before any major update, driver change, or distro upgrade — not just on a schedule.",
      "Follow the 3-2-1 rule where practical: at least 3 copies, on 2 different media types, with 1 copy offsite.",
      "If using Timeshift with rsync mode, exclude large media directories to keep snapshot size and time manageable.",
    ],
    commonIssues: [
      { issue: "Snapshot restore doesn't bring back personal files", fix: "Expected behavior for most tools by default — check your snapshot tool's include/exclude list and add /home explicitly, or run a separate file backup for personal data." },
      { issue: "Btrfs snapshots filling up disk space over time", fix: "Old snapshots aren't automatically freed — set a retention policy (Timeshift and snapper both support this) and prune manually with `sudo btrfs subvolume delete` if needed." },
    ],
    tipsAndTricks: ["Boot into GRUB and pick a Timeshift/snapper snapshot entry directly (both add boot menu integration on btrfs) to recover a system that won't boot at all, without needing a live USB."],
    faqs: [{ q: "Is Timeshift a full backup solution?", a: "It's a system-recovery tool, not a full backup — it protects against a bad update or config change, but you still need a separate personal-file backup (and ideally an offsite/cloud copy) for real data-loss protection." }],
    relatedSettingIds: ["linux-storage-disk-management", "linux-troubleshooting-boot-issues"],
  },
  {
    id: "linux-printing-setup",
    title: "Printing Setup (CUPS)",
    icon: Printer,
    platform: "linux",
    category: "devices-peripherals",
    frequentlyUsed: false,
    difficulty: "Easy",
    estimatedTime: "4 min read",
    supportedVersions: "Most desktop distributions (Ubuntu, Fedora, Debian, Arch, and derivatives)",
    heading: "Add and manage printers through CUPS",
    description:
      "Nearly all Linux distros use CUPS (Common Unix Printing System) under the hood for printing, whether you add a printer through a graphical Settings panel or its own web interface — the two manage the exact same configuration.",
    details: [
      "GUI: Settings > Printers (GNOME/KDE) usually auto-detects printers on the local network via mDNS/Bonjour — click Add and select from the list.",
      "CUPS web interface: open a browser to `http://localhost:631` for full control over printers, queues, and sharing — works identically across distros regardless of desktop environment.",
      "Install CUPS if it's missing: `sudo apt install cups` / `sudo dnf install cups` / `sudo pacman -S cups`, then enable it with `sudo systemctl enable --now cups`.",
      "Driverless printing (IPP Everywhere/AirPrint-compatible printers) works out of the box on most modern printers without installing a manufacturer driver at all.",
    ],
    redirectUrl: "https://openprinting.github.io/cups/",
    actionLabel: "Open CUPS Documentation",
    whyItMatters:
      "Printing is one of the few remaining areas where Linux depends heavily on the specific printer's driver support — knowing that CUPS is the common layer underneath every distro's printer settings means the same troubleshooting steps and web interface work regardless of which desktop environment you're on.",
    bestPractices: [
      "Check if your printer supports driverless IPP printing before hunting for a manufacturer driver — it's plug-and-play on both wired and network printers when supported.",
      "For older printers, check OpenPrinting's database (openprinting.org) for a known-working driver before assuming it's unsupported.",
      "Add network printers by IP address in CUPS if auto-discovery doesn't find them — more reliable across subnets and VLANs.",
    ],
    commonIssues: [
      { issue: "Printer detected but jobs stay stuck in the queue", fix: "Check the queue status at `http://localhost:631`, and restart the CUPS service with `sudo systemctl restart cups` — a stuck job or spooler issue is the most common cause." },
      { issue: "Printer not detected on the network at all", fix: "Confirm the printer and computer are on the same subnet, and that `avahi-daemon` (mDNS discovery) is installed and running — most auto-detect failures trace back to mDNS traffic being blocked or not running." },
    ],
    tipsAndTricks: ["`lpstat -p -d` from a terminal gives a quick summary of configured printers and which one is the default, faster than opening the GUI."],
    faqs: [{ q: "Why isn't my printer's manufacturer driver available for Linux?", a: "Many manufacturers don't ship Linux drivers at all, but most printers made in the last decade support driverless IPP printing, which CUPS handles natively — check openprinting.org's database for your specific model before assuming it won't work." }],
    relatedSettingIds: ["linux-flatpak-snap-apps", "linux-wifi-network"],
  },
  {
    id: "linux-flatpak-snap-apps",
    title: "Flatpak & Snap App Management",
    icon: LayoutGrid,
    platform: "linux",
    category: "apps-features",
    frequentlyUsed: true,
    difficulty: "Easy",
    estimatedTime: "4 min read",
    supportedVersions: "Most desktop distributions (Ubuntu, Fedora, Debian, Arch, and derivatives)",
    heading: "Install and manage apps outside your distro's native packages",
    description:
      "Flatpak and Snap are sandboxed, distro-agnostic app formats that sit alongside your native package manager — Flatpak (via Flathub) is more broadly adopted across distros, while Snap is maintained by Canonical and preinstalled on Ubuntu by default.",
    details: [
      "Flatpak: install support with `sudo apt install flatpak` / `sudo dnf install flatpak` (Fedora ships it by default) / `sudo pacman -S flatpak`, then add Flathub with `flatpak remote-add --if-not-exists flathub https://flathub.org/repo/flathub.flatpakrepo`.",
      "Install a Flatpak app: `flatpak install flathub <app-id>`; update all: `flatpak update`; both also work through most \"Software\" GUI apps that already have Flathub configured.",
      "Snap (preinstalled on Ubuntu; installable elsewhere via `sudo apt/dnf/pacman install snapd`): `sudo snap install <package>`; updates happen automatically in the background, or manually via `sudo snap refresh`.",
      "Both formats sandbox apps and may need explicit permission grants for filesystem, camera, or microphone access — Flatpak via `flatpak permissions` or the Flatseal GUI app, Snap via `snap connections`.",
    ],
    redirectUrl: "https://docs.flatpak.org/en/latest/",
    actionLabel: "Open Flatpak Documentation",
    whyItMatters:
      "Flatpak and Snap solve the long-standing problem of an app needing a newer library version than your distro ships — they let up-to-date app versions install cleanly on any distro without touching system packages, at the cost of larger download sizes and occasional sandboxing quirks.",
    bestPractices: [
      "Prefer your distro's native package for core system tools, and Flatpak/Snap for user-facing apps (browsers, chat clients, creative tools) where the newest version matters more.",
      "Periodically clean up unused runtimes with `flatpak uninstall --unused` — Flatpak keeps shared runtime dependencies that can accumulate over time.",
      "If a sandboxed app can't see a file or folder you expect, check its permissions (Flatseal for Flatpak) rather than assuming it's a bug.",
    ],
    commonIssues: [
      { issue: "Flatpak app can't access files outside the Home/Downloads folder", fix: "Grant broader filesystem access with Flatseal, or run `flatpak override --filesystem=<path> <app-id>` for a specific folder." },
      { issue: "Snap apps feel slow to launch", fix: "This is a known characteristic of Snap's compression/mounting approach, especially on the first launch after boot — subsequent launches are faster since the squashfs stays mounted." },
    ],
    tipsAndTricks: ["`flatpak list` and `snap list` show everything currently installed through each system — useful before a reinstall to know what to restore."],
    faqs: [{ q: "Should I remove Snap and only use Flatpak, or vice versa?", a: "Not necessary — both can coexist without conflict. Pick whichever has the app you need; on Ubuntu some apps (like Firefox in recent releases) ship as Snap by default, which you can swap for the Flatpak or native .deb version if preferred." }],
    relatedSettingIds: ["linux-desktop-environment-customization", "linux-system-update"],
  },
  {
    id: "linux-troubleshooting-boot-issues",
    title: "Troubleshooting Boot Issues",
    icon: Terminal,
    platform: "linux",
    category: "troubleshooting-diagnostics",
    frequentlyUsed: false,
    difficulty: "Intermediate",
    estimatedTime: "6 min read",
    supportedVersions: "Most desktop distributions (Ubuntu, Fedora, Debian, Arch, and derivatives)",
    heading: "Recover a system that won't boot normally",
    description:
      "A Linux system that won't boot almost always still gives you a path back in — GRUB's boot menu, recovery mode, or a live USB — the key is knowing which layer is failing (bootloader, kernel, or graphical session) so you fix the right thing instead of reinstalling.",
    details: [
      "At the GRUB menu (hold Shift or Esc during boot if it's hidden), select \"Advanced options\" for a recovery mode boot or an older kernel version — useful if the latest kernel or driver update broke booting.",
      "If it boots to a black screen instead of the login screen, switch to a TTY with Ctrl+Alt+F3, log in, and check `journalctl -b -1` (previous boot's logs) or `journalctl -xb` (current boot) for the specific failure.",
      "A live USB of your distro (or any distro) lets you mount the broken system's partition (`sudo mount /dev/sdXN /mnt`) and inspect or fix config files, back up data, or chroot in to repair GRUB.",
      "Reinstall/repair GRUB from a live USB after chrooting: `sudo grub-install /dev/sdX && sudo update-grub` (Debian/Ubuntu) or `sudo grub2-mkconfig -o /boot/grub2/grub.cfg` (Fedora).",
    ],
    important: "Don't panic-reinstall — booting a live USB and reading the actual error in `journalctl` takes a few minutes and usually reveals a fixable one-line cause (bad fstab entry, broken driver, misconfigured GRUB) rather than a reason to start over.",
    redirectUrl: "https://www.gnu.org/",
    actionLabel: "Open GRUB & Boot Documentation",
    whyItMatters:
      "A failed boot is the most stressful Linux troubleshooting scenario for most people because it looks catastrophic, but the underlying causes (bad fstab entry, broken driver, misconfigured GRUB, full disk) are almost always specific and fixable once you can see the actual error instead of just a black screen.",
    afterImageContent: {
      heading: "Confirm the system boots cleanly going forward",
      steps: [
        "Boot normally (not into recovery mode) after applying a fix, to confirm it resolved the issue rather than just working around it.",
        "Check `journalctl -b` for new errors even on a successful boot — some issues degrade rather than fully block booting.",
        "Take a snapshot or note the working kernel/driver version once confirmed stable, so there's a known-good rollback point next time.",
      ],
    },
    bestPractices: [
      "Keep at least one older kernel installed (most distros do this by default) as a fallback if a new kernel breaks boot.",
      "Have a live USB of your distro prepared in advance rather than needing to create one during an actual emergency without another working computer.",
      "Take a system snapshot (see Backup & Snapshots) before major updates so a broken boot is a one-command rollback instead of a manual repair.",
    ],
    commonIssues: [
      { issue: "\"Failed to mount\" or emergency mode (initramfs prompt) at boot", fix: "Almost always a bad `/etc/fstab` entry — from initramfs, remount root read-write (`mount -o remount,rw /`), edit fstab, and reboot." },
      { issue: "GRUB menu missing entirely, boots straight past it", fix: "Hold Shift (BIOS) or Esc (UEFI) right after power-on to force the menu open, or reinstall GRUB from a live USB if it's not appearing at all." },
    ],
    tipsAndTricks: ["`journalctl -b -1 -p err` filters the previous boot's log down to just error-level messages, cutting out a lot of noise when hunting for the actual cause."],
    faqs: [{ q: "Will reinstalling fix a boot problem faster than troubleshooting it?", a: "Rarely — most boot failures are a single misconfigured file or driver, fixable in a few minutes from a live USB, while a reinstall costs much more time and risks personal data if backups aren't current." }],
    relatedSettingIds: ["linux-backup-snapshots", "linux-storage-disk-management"],
  },
];
