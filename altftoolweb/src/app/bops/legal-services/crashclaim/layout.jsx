export const metadata = {
  title: "CrashClaim Law — Car Accident Claim Reviews",
  description: "After a crash, do not face the insurers alone — free claim review within 24 hours.",
  robots: { index: false, follow: true, googleBot: { index: false, follow: true } },
  alternates: { canonical: "/bops/legal-services/crashclaim" },
};

export default function CrashClaimLayout({ children }) {
  return children;
}
