export default function HousingServicesLayout({ children }) {
  // The parent /bops layout supplies the one truthful, consistent demo notice
  // and disables placeholder quote/call controls for every nested family.
  return children;
}
