export const metadata = {
  title: "GuardianAlert — Medical Alert Systems",
  description: "One-button help, fall detection and 24/7 response — independence with a safety net.",
  robots: { index: false, follow: true, googleBot: { index: false, follow: true } },
  alternates: { canonical: "/bops/senior-care/guardianalert" },
};

export default function GuardianAlertLayout({ children }) {
  return children;
}
