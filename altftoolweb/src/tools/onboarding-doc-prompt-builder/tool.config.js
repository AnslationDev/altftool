const toolConfig = {
  slug: "onboarding-doc-prompt-builder",
  name: "Onboarding Doc Prompt Builder",
  category: ["Business"],
  description:
    "Turns a role, team and access list into a first-week onboarding document prompt with an hour-by-hour split across the standard sections.",
  icon: "user-plus",
  iconColor: "text-[var(--primary)]",
};

export default toolConfig;
