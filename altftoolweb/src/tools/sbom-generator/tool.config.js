const toolConfig = {
  slug: "sbom-generator",
  name: "SBOM Generator",
  category: ["Developer", "Security"],
  description:
    "Generate a local CycloneDX JSON SBOM from bounded npm manifest or lockfile data with explicit incomplete-inventory and metadata policies.",
  icon: "list-tree",
  iconColor: "text-primary",
  wideWorkspace: true,
};

export default toolConfig;
