const toolConfig = {
  slug:"animation-generator",
  // Two tools shipped the display name "Animation Generator" — the only
  // duplicate name across all 3,947 — in the same category, so listings,
  // cards and search showed the same label twice and neither said what it
  // did. This one builds @keyframes by hand, which its own seo.js title
  // already states; the name now agrees with it.
  name: "CSS Keyframes Animation Builder",
  category:"Creators",
  description:"Generate  animations for your projects in easy way  ",
  icon: "sparkle",
  iconColor: "text-teal-500",
}

export default toolConfig;
