const toolConfig = {
  slug: "email-authentication-checker",
  name: "Email Authentication Checker",
  category: ["Email Marketing"],
  description:
    "Check a domain's SPF, DKIM, DMARC and MX records live in your browser and get a clear pass/warn/fail verdict with fixes — the deliverability-critical checks that decide whether your email reaches the inbox.",
  icon: "shield-check",
  iconColor: "text-emerald-600",
  wideWorkspace: true,
};

export default toolConfig;
