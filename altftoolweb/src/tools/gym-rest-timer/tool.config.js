const toolConfig = {
  slug: "gym-rest-timer",
  name: "Gym Rest Timer",
  category: ["Health & Fitness"],
  description:
    "Per-exercise rest countdowns that auto-advance through your sets, with rest presets for strength, hypertrophy and endurance.",
  icon: "timer",
  iconColor: "text-[var(--primary)]",
};

export default toolConfig;
