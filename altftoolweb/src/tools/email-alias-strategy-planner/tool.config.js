const toolConfig = {
  slug: "email-alias-strategy-planner",
  name: "Email Alias Strategy Planner",
  category: ["Security & Privacy"],
  description:
    "Design per-service email aliases so a leaked address names the company that leaked it, with RFC length checks on every address.",
  icon: "at-sign",
  iconColor: "text-[var(--primary)]",
};

export default toolConfig;
