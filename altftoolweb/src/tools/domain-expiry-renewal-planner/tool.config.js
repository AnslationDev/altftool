const toolConfig = {
  slug: "domain-expiry-renewal-planner",
  name: "Domain Expiry and Renewal Planner",
  category: ["Business"],
  description:
    "Dates every stage of the gTLD expiry lifecycle — grace, redemption, pending delete and drop — plus transfer locks and renewal risks.",
  icon: "calendar-clock",
  iconColor: "text-[var(--primary)]",
};

export default toolConfig;
