import toolConfig from "./tool.config";

const seo = {
  title: `${toolConfig.name} - ALTFTool`,
  description: toolConfig.description,
  keywords: ["Malayalam birthday wishes", "Malayalam birthday message", "birthday greeting generator"],
};

export default seo;
