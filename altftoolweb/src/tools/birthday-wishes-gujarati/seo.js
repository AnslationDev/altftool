import toolConfig from "./tool.config.js";

const seo = {
  title: `${toolConfig.name} - ALTFTool`,
  description: toolConfig.description,
  keywords: ["Gujarati birthday wishes", "Gujarati birthday message", "birthday greeting generator"],
};

export default seo;
