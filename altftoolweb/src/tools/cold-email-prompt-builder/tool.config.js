const toolConfig = {
  slug: "cold-email-prompt-builder",
  name: "Cold Email Prompt Builder",
  category: ["Business"],
  description:
    "Generates outreach prompts with persona, trigger event and one clear ask — sized to the 50-125 word range that gets replies.",
  icon: "send",
  iconColor: "text-[var(--primary)]",
};

export default toolConfig;
