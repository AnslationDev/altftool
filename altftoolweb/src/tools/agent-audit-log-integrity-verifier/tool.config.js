const toolConfig = {
  slug: "agent-audit-log-integrity-verifier",
  name: "Agent Audit Log Integrity Verifier",
  category: ["Security & Privacy", "Developer", "AI Tools"],
  description:
    "Check JSON or JSONL agent audit logs locally for structural continuity and optionally recompute an explicitly configured SHA-256 canonical hash chain.",
  icon: "shield-check",
  iconColor: "text-primary",
  wideWorkspace: true,
};

export default toolConfig;
