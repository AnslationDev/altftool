const toolConfig = {
  slug: "subscription-wind-down-planner",
  name: "Subscription Wind-Down Planner",
  category: ["Security & Privacy"],
  description:
    "List recurring subscriptions, project what they cost if nobody cancels, and record where each one can be stopped.",
  icon: "list-checks",
  iconColor: "text-[var(--primary)]",
};

export default toolConfig;
