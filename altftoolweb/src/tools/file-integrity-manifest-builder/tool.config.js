const toolConfig = {
  slug: "file-integrity-manifest-builder",
  name: "File Integrity Manifest Builder",
  category: ["Security & Privacy", "Developer", "Productivity"],
  description:
    "Create a deterministic local SHA-256 manifest for bounded file sets, compare it with a previous manifest, and review byte changes and duplicate digests without uploading files.",
  icon: "files",
  iconColor: "text-primary",
  wideWorkspace: true,
};

export default toolConfig;
