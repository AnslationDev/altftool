const toolConfig = {
  slug: "pan-format-validator",
  name: "PAN Format Validator",
  category: ["Finance", "Business", "Utility"],
  description:
    "Validate Indian PAN number format, normalize entries, identify holder type, mask output, and check bulk PAN lists locally.",
  icon: "id-card",
  iconColor: "text-blue-600",
};

export default toolConfig;
