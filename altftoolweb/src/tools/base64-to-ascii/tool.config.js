const toolConfig = {
  slug: "base64-to-ascii",
  name: "Base64 to ASCII",
  category: ["Developer", "Converter"],
  description: "Decode Base64 encoded strings into readable ASCII text instantly.",
  icon: "binary",
  iconColor: "text-blue-600",
  // Full-bleed converter layout — skip the narrow workspace + side ad rails.
  wideWorkspace: true,
};

export default toolConfig;
