import toolConfig from "./tool.config";

const seo = {
  title: `${toolConfig.name} - ALTFTool`,
  description: toolConfig.description,
  keywords: [
    "Spanish New Year wishes",
    "Feliz Año Nuevo messages",
    "Spanish greeting generator",
    "ALTFTool",
  ],
};

export default seo;
