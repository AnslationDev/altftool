const toolConfig = {
  slug: "time-zone-meeting-planner",
  name: "Time Zone Meeting Planner",
  category: ["Productivity"],
  description:
    "Finds the times on a chosen day that fall inside working hours for every participant's time zone, using the browser's IANA time zone data.",
  icon: "calendar-clock",
  iconColor: "text-primary",
};

export default toolConfig;
