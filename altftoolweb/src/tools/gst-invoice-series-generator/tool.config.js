const toolConfig = {
  slug: "gst-invoice-series-generator",
  name: "GST Invoice Series Generator",
  category: ["Generators"],
  description:
    "Build a consecutive tax invoice numbering series for a financial year and check it against the 16-character limit in Rule 46(b).",
  icon: "hash",
  iconColor: "text-[var(--primary)]",
};

export default toolConfig;
