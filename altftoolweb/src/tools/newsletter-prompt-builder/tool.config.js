const toolConfig = {
  slug: "newsletter-prompt-builder",
  name: "Newsletter Prompt Builder",
  category: ["Marketing & Social"],
  description:
    "Drafts a newsletter prompt with segment, cadence and CTA guidance — subject-line limits and a per-item word budget built in.",
  icon: "mail",
  iconColor: "text-[var(--primary)]",
};

export default toolConfig;
