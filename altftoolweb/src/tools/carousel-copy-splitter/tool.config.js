const toolConfig = {
  slug: "carousel-copy-splitter",
  name: "Carousel Copy Splitter",
  category: ["Text & Writing"],
  description:
    "Split long copy into balanced carousel slides on sentence boundaries, with a hook slide, a call-to-action slide and per-platform slide limits.",
  icon: "gallery-horizontal-end",
  iconColor: "text-[var(--primary)]",
};

export default toolConfig;
