const toolConfig = {
  slug: "media-metadata-timeline-correlator",
  name: "Media Metadata Timeline Correlator",
  category: ["Image & Photo", "Developer", "Security & Privacy"],
  description:
    "Extract bounded date fields from local image, MP4, and WAV metadata, preserve timezone uncertainty, and compare the resulting timeline without authenticity claims.",
  icon: "clock",
  iconColor: "text-primary",
  wideWorkspace: true,
};

export default toolConfig;
