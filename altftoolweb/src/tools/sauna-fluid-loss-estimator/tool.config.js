const toolConfig = {
  slug: "sauna-fluid-loss-estimator",
  name: "Sauna Fluid Loss Estimator",
  category: ["Health & Fitness"],
  description:
    "Estimate sweat lost per sauna session from weight change or temperature and duration, plus the volume to drink back.",
  icon: "flame",
  iconColor: "text-[var(--primary)]",
};

export default toolConfig;
