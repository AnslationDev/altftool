"use client";

import GeneratedToolPage from "@/tools/_shared/GeneratedToolPage";
import toolConfig from "../tool.config";

export default function ToolHome() {
  return <GeneratedToolPage toolConfig={toolConfig} />;
}
