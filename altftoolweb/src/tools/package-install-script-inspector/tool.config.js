const toolConfig = {
  slug: "package-install-script-inspector",
  name: "Package Install-Script Inspector",
  category: ["Developer", "Security"],
  description:
    "Inspect package.json lifecycle commands as inert text for network, file, shell, process, environment, and native-build review cues.",
  icon: "package-check",
  iconColor: "text-primary",
  wideWorkspace: true,
};

export default toolConfig;
