const toolConfig = {
  slug: "keyboard-focus-order-replay",
  name: "Keyboard Focus-Order Replay",
  category: ["Developer"],
  description:
    "Estimate sequential keyboard focus order from pasted static HTML and replay the resulting tab stops locally with calibrated structural cues.",
  icon: "keyboard",
  iconColor: "text-primary",
  wideWorkspace: true,
};

export default toolConfig;
