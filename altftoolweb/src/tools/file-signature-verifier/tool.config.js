const toolConfig = {
  slug: "file-signature-verifier",
  name: "File Signature Verifier",
  category: ["Security & Privacy", "Developer", "File"],
  description:
    "Compare a local file's leading magic bytes with its extension and browser-reported MIME type without uploading the file.",
  icon: "file-search",
  iconColor: "text-primary",
  wideWorkspace: true,
};

export default toolConfig;
