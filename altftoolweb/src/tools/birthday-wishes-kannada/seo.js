import toolConfig from "./tool.config.js";

const seo = {
  title: `${toolConfig.name} - ALTFTool`,
  description: toolConfig.description,
  keywords: ["Kannada birthday wishes", "Kannada birthday message", "birthday greeting generator"],
};

export default seo;
