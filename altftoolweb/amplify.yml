version: 1
frontend:
  phases:
    preBuild:
      commands:
        - nvm use 24
        - node scripts/write-amplify-runtime-env.mjs
        - npm ci --no-audit --no-fund
    build:
      commands:
        - export ALTFT_DEFER_BULK_PRERENDER=true
        - export ALTFT_NODE_MAX_OLD_SPACE_SIZE=8192
        - npm run build
        - rm -rf .next/cache
  artifacts:
    baseDirectory: .next
    files:
      - "**/*"
